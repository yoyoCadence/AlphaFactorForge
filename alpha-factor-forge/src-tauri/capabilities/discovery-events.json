{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "discovery-events",
  "description": "Allow only the main window to subscribe to backend discovery progress, result, and terminal events.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
