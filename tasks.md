# AlphaFactorForge Tasks

This is the single active task board for the workspace. The former `task.md` legacy feature map has been merged into this file and should not be recreated as a second task board.

Task lifecycle: **Backlog -> Next -> In Progress -> Done**.

## Current Snapshot

- P10 publication: implementation commit `9cbf0ee` and its acceptance-review follow-up were merged through [PR #113](https://github.com/yoyoCadence/AlphaFactorForge/pull/113) as `ccb60e1` on 2026-09-22. P11 was branched from this fetched merged baseline.

- **P11 executable DSL published for review (2026-09-22)**: implementation `c1391f2` plus acceptance-review follow-up `22c8739` are pushed in Chinese Draft [PR #114](https://github.com/yoyoCadence/AlphaFactorForge/pull/114); merge remains maintainer-owned. `strategy-dsl-v1` now has strict TypeScript/Rust validation and deterministic evaluation over the unambiguous indicator intersection, with arity/type/finite-value/parameter/lookback/causality/depth/node guards and one hand-authored parity fixture. Six focused acceptance-review groups make both evaluators exercise every v1 indicator/operator and require varying entry/exit signals; temporal offsets accept `[1,400]` while indicator periods remain `[2,400]`. `discovery-config-v1` remains params-only; the versioned `discovery-config-v2` admits fixed DSL candidates only after validation, forbids axes, freezes execution/risk/cost fields outside the tree, revalidates execution and embargo allowance, and persists `dsl_json`, parameter schema, hypothesis, and attempt lineage before worker execution. The backend `validate_strategy_dsl` command is live; AI generation/approval remains P15. **973 Vitest / 373 Rust (75 + 296 + 2) / 78 Playwright**, typecheck/build/`cargo check --locked --all-targets` pass. Normal clippy has only the 5 existing warnings; `-D warnings` fails on those unchanged files. No migration or dependency change. [Contract](docs/strategy-dsl-contract.md), [handoff](handoffs/2026-09-22-p11-executable-dsl-v1.md).

- **P10 FinMind/TWSE TW ETF adapter completed locally (2026-09-21)**: bounded `fetch-tw-etf` CLI for 0050/006208/0056/00878/00713, FinMind raw OHLCV primary, independent TWSE monthly-quote comparison, 2025 trading calendar, dividends/payment dates, 0050 4:1 split and suspension evidence, immutable originals/revisions/reports and P06 snapshots. Real public-source smoke for 2025-06-09..21 produced complete requested coverage for all five symbols (0050 5/5; the others 10/10); all snapshots intentionally remain degraded only because operator costs are unconfirmed. **969 Vitest / 366 Rust (71 + 293 + 2) / 78 Playwright**, typecheck/build/check pass; clippy has only the 5 existing warnings. No schema, dependency, engine or UI change. [Contract](docs/market-source-tw-etf-v1.md), [handoff](handoffs/2026-09-21-p10-tw-etf-v1.md).

- P09 publication: implementation commit `8ed8f80` and review record `1d1ca34` were merged through Chinese [PR #111](https://github.com/yoyoCadence/AlphaFactorForge/pull/111) as `396ee56` on 2026-09-21; all six CI lanes passed. This remote merge does not complete the separate authenticated-source acceptance: live Tiingo entitlement, coverage and payment-date evidence still await the operator token.

- **P09 implementation and local validation completed (2026-09-21)**: Tiingo EOD/distribution adapter, Windows Credential Manager, bounded `fetch-tiingo` CLI, five-symbol eligibility, immutable raw/revision/report storage, full-range audit and P08 payment/cost gates. **969 Vitest / 353 Rust (71 + 280 + 2) / 78 Playwright**, typecheck/build/check pass; clippy has only the 5 existing warnings. Native CLI records five explicit `credential_missing` blockers in an isolated workspace. **P09 authenticated external acceptance remains In Progress pending the operator's token**; no live coverage/entitlement claim. [Contract](docs/market-source-tiingo-v1.md), [handoff](handoffs/2026-09-21-p09-tiingo-v1.md). P10 has since completed separately.

- P08 publication: [PR #110](https://github.com/yoyoCadence/AlphaFactorForge/pull/110) merged as `4e48343`, verified by fetch at P09 start (2026-09-21).

- **P08 ETF daily semantics completed (2026-09-20)** on `feat/p08-etf-market-semantics`: **969 Vitest**, **337 Rust** (71＋264＋2), **78/78 Playwright**, typecheck/frontend build and `cargo check --locked --all-targets` pass. Clippy has only the same 5 existing warnings. Adds opt-in `etf-semantics-v1` / `etf-metrics-v1`, with 55 shared specification cases for calendars, dividends/payments, splits/orders, causal signal prices, native currency/costs and elapsed-time CAGR. No schema/dependency or legacy-engine change. No native desktop UI smoke in this pure-core phase; native service smoke and existing browser regression pass. [Contract](docs/etf-semantics-v1.md), [handoff](handoffs/2026-09-20-p08-etf-semantics-v1.md). ETF adapters have since landed in P09/P10; shared execution integration remains P18 and P09 still needs authenticated acceptance.

- P07 acceptance fixes completed locally (2026-09-20, PR #109): **335 Rust** (69＋264＋2), **911 Vitest**, build/typecheck and `cargo check --locked --all-targets` pass; clippy has only the same 5 existing warnings. Seven added regressions fail on the pre-fix HEAD and pass with the fixes. Archive availability now records response completion; old metadata cannot enter forward-observed snapshots or continue to qualify through snapshot readers. CHECKSUM originals are linked to every ZIP attempt and revalidated on cache reuse. Real old-cache upgrade and the next cached fetch both return **96/96** across the timestamp-unit boundary. Browser/native UI was not rerun for these backend-only fixes; prior P07 review verified 78/78 Playwright. See the [review Resolution](handoffs/2026-09-20-pr109-p07-acceptance-review-v1.md#resolution).
- P07 (Binance crypto adapter) landed on `feat/p07-binance-adapter`, draft PR [#109](https://github.com/yoyoCadence/AlphaFactorForge/pull/109) (2026-09-20): Rust **328** (69＋257＋2), Vitest **911**, Playwright **78/78**, typecheck/build/`cargo check --locked --all-targets` clean, clippy unchanged (the 5 pre-existing warnings); PR #109 CI green on all six lanes after the fixture files themselves had to be committed past the repository-wide `*.zip` ignore. **The first phase that retrieves real market data**: BTC/ETH 1h all of 2025 = 8760/8760 bars each, the archive's millisecond→microsecond change handled inside one range, the current month served by daily archives + one REST page, and a real pre-listing gap (1132 bars) reported and refused. Adds `ureq 3.4.2` + `flate2 1.1` (13 crates into the lock) — the first network dependency in the workspace. Details and the honest coverage limits: `docs/market-source-binance-v1.md`.
- P06 acceptance fixes completed locally (2026-09-20): all four [review findings](handoffs/2026-09-20-p06-acceptance-review-v1.md) resolved, with 5 new Rust regression tests. Current verification: Rust **302** (68＋232＋2), Vitest **909**, typecheck/build/`cargo check --locked --all-targets` passed; clippy retains only the 5 pre-existing warnings. Playwright/native UI and remote CI were not rerun for these backend-only fixes; the PR/CI result below describes the preceding committed revision.
- P06 (market data foundation) landed on `feat/p06-market-foundation`, draft PR [#108](https://github.com/yoyoCadence/AlphaFactorForge/pull/108) (2026-09-20): Rust **297** (68＋227＋2), Vitest **909**, Playwright **78/78**, typecheck/build/`cargo check` clean, clippy unchanged (the four existing core warnings plus the one in `file_commands.rs`); PR #108 CI green on all six lanes including native-smoke. These are the pre-acceptance-fix suite counts; the latest local verification is recorded above. P06 adds no dependency, no network access, and no change to any existing dataset, migration, or contract.
- P05 acceptance follow-up verified on PR #107 (2026-09-20): Rust **261** (52＋207＋2), Vitest **879**, Playwright **78/78**, typecheck/build/cargo check clean.
- GitHub synchronization restored (2026-09-19): P00–P04 and acceptance fixes published in draft PR [#106](https://github.com/yoyoCadence/AlphaFactorForge/pull/106), rebased onto main `97eaf9e`. Original hashes retained on `archive/local-before-github-sync-2026-09-19`; all 24 local branches' histories are present on origin. Post-rebase Rust **250**, Vitest **879**, build/typecheck/check pass; focused browser verification recorded in `handoffs/2026-09-19-github-sync-v1.md`. PR remains draft, not merged.

- Latest P04b H2 follow-up (2026-09-19): first-page ledger gaps now request reconciliation; the panel subscribes before its initial snapshot and queues notifications received during reads. Rust **247**, Vitest **879**, typecheck/build/check pass; clippy only the four existing core warnings. Playwright **75 verified**: full run 74 passed plus one cold-navigation timeout, that test passed on focused rerun. See the final Resolution in `handoffs/2026-09-18-p04b-desktop-connect-v1.md`.

- Latest P04a acceptance verification (2026-09-18): four findings fixed with regressions; Rust **236** (52 + 182 + 2), Vitest **874**, typecheck/build/cargo check pass; clippy has only the four existing core warnings. See the Resolution in `handoffs/2026-09-17-p04a-headless-service-v1.md`.

- Legacy prototype: `AlphaFactorForge.dc.html` is a feature-rich browser PWA and remains the UI/behavior reference.
- Target app: `alpha-factor-forge/` is the Tauri Desktop Phase A scaffold.
- Baseline verified: `npm test`, `npm run typecheck`, and `npm run build` pass in `alpha-factor-forge/`.
- Native Tauri verified: Rust 1.96 / Cargo / MSVC build tools / Tauri CLI v2 installed; `cargo check` and `cargo tauri dev` both pass; multi-size icons generated.
- Progress (through RUNNER-EXEC-001 + RUNNER-STORE-001 + RUNNER-CONFIG-001 + RUNNER-OWNERSHIP-001 + RS-CORE-005 + IDENTITY-001 + PERSIST-001 + PERSIST-AUDIT-001 + FEAT-002 + code-mode UX polish + REF-004 + BUG-004 + UI-port Slice 8b-2): Phase A backtest pipeline and transactional SQLite persistence (datasets, candles, strategies, summaries, and closed trades); versioned SHA-256-only strategy/dataset content identity with TS/Rust exact fixtures, backend verification, and atomic immutable dataset import; Phase B's pure deterministic Train/Validation/Test + embargo split contract, its Train/Validation segmented backtest runner (Test never executed), usage-aware embargo derivation (max used-signal lookback + explicit holding allowance), the full §6 benchmark suite (Buy & Hold / SMA 50/200 / RSI 14 30-70 / Bollinger 20-2 + seeded Random Entry Monte Carlo with matched holding periods), the §5.1 hard elimination gate (explicit thresholds, fail-closed evidence), corrected Sortino/Calmar + explicit non-finite persistence (METRIC-001), the §5.2 score-v1 ranking breakdown (SCORE-001, per the PR #61 handoff Resolution), immutable validation-record persistence (PERSIST-001, per the PR #64 handoff Resolution: migration 0002, atomic bundle save, shared metrics codec), and committed TypeScript-reference/Rust parity through indicators, backtest/metrics, params signals/split/embargo, deterministic benchmarks, mulberry32/Random Entry, Gate, and params-only Score, plus the discovery run's admission contract (RUNNER-CONFIG-001: strict `discovery-config-v1` parsing, params-only candidate enumeration/pruning/`strategy-v2` deduplication, hash-sorted stable indexes, `seed-v1` sub-seeds, and the 256/4096 candidate caps), its run/job store (RUNNER-STORE-001: migration 0003, the D5 state machine with one non-terminal run enforced in the database, the one-transaction candidate commit spanning summaries/trades/record/both job rows/progress/lifecycle, derived promotion, and idempotent crash recovery), and its executable backend runner (RUNNER-EXEC-001: fixed CPU pool, single SQLite coordinator/writer, lifecycle controls, checkpointed progress, and post-commit versioned events), protected by a plugin-first desktop single-instance guard that exits secondary launches before SQLite setup/recovery and restores the primary window (RUNNER-OWNERSHIP-001); chart (canvas + overlays + trade markers + wheel-zoom + drag-pan + hover + bar replay); params/blocks/code strategy modes with invalid-expression Run guard; holdout; parameter sweep + interactive heatmap; report export (Slice 7-2); SQLite strategy library (Slice 7-3); native chart + metrics OS windows (Slice 8b); mutable-field strategy UPSERT semantics (REF-004); plus the 2026-07-07 project audit (`docs/` blueprint) and its backlog work: DOC-001, BUG-001, REF-001→004, TEST-001→002 (browser E2E flows, golden lock, and legacy parity), and Backtest Correctness Phases 1–3 (fee-inclusive accounting, settled metrics, execution-bar/risk fills, legacy `both` reversal, and normalized-fraction validation); plus BUG-RESULT-CONTEXT-001, which binds each completed interactive backtest to an immutable strategy/dataset/range artifact that Save and Export are the only readers of, and BUG-SWEEP-CONTEXT-001, which does the same for the parameter sweep (`sweep-context-v1`: dataset identity, optimised range including the Holdout split, sweep config, and a swept-axis-masked base strategy). plus STRATEGY-VALIDATION-001, the one runtime admission gate that stops an indicator parameter which cannot produce a series (`0`, `2.5`, negative, non-finite) from running or being persisted as a confident zero-trade result. plus RUNNER-UI-001a, the typed `discovery-event-v1` frontend boundary pinned from both languages against one authored fixture, RUNNER-UI-001b-1, the run-config envelope builder that pre-validates through the shared admission parser so a malformed run never reaches a command, and RUNNER-UI-001b-2, the runner panel that starts/pauses/resumes/cancels a run, adopts one that already exists, and streams throttled progress and results. plus SKIN-001, the ten-skin interface theme (a `Theme` token contract every component reads through `useTheme()`, canvas painting driven by `chart.style`, and a header picker whose choice persists and reaches the pop-out windows). plus P01, the Results Explorer (a read-only viewer over `validation_records` / `backtest_summary` / `trades` with Validation ranking, Test hidden, on-demand loading, and honest missing-history messages, backed by the new `get_backtest_result_detail` read command and `discovery_run_id` on the typed record DTO; the two findings of the 2026-09-17 acceptance review — trades attached to a since-replaced summary, and an automatic retry loop after a failed read — are fixed with regressions). plus P02, the runtime decoupling (ABC-02a): the runner and database no longer name Tauri — the desktop event sink moved to `desktop/discovery_events.rs`, `db::open_at(path)` replaced the `AppHandle`-bound initializer and sets the 5 s busy timeout, and `runtime::open_workspace(path)` is the one open → migrate → runner → orphan-recovery path the desktop and a future headless host share, guarded by a source-scan boundary test. plus P03a, workspace ownership: the desktop now OWNS its workspace in the contract's order (OS lock on `ownership.lock` → open → migrate → ownership epoch in `workspace_ownership` (migration 0004) → orphan recovery → 5 s heartbeat), every runner store write (including claim and strategy/run/progress admission) checks the lease epoch inside a BEGIN IMMEDIATE transaction, and a database written by a newer build is refused before anything runs. plus P03b, the versioned command envelope and event ledger: `research-command-v1` envelopes are dispatched by a host-agnostic `runtime::commands::Dispatcher` (exact protocol version, workspace check, command whitelist, and reserve-then-complete idempotency by `requestId` in `command_requests`), every runner event is appended to the persistent `runtime_events` ledger (`research-event-v1`, monotonic `eventId` cursor) before the desktop sees it, and the desktop exposes `dispatch_research_command` / `get_workspace_info` (migration 0005 also mints the stable `workspace_id`). Current local tests: 969 Vitest + 337 Rust + 78/78 Playwright (P08, 2026-09-20; see Current Snapshot and P08 handoff).
- The latest full local count supersedes the historical P08 count embedded in the long progress inventory above: **969 Vitest + 366 Rust + 78/78 Playwright** (P10, 2026-09-21).
- Security snapshot (2026-07-16): SEC-002 exact-pins Vite 6.4.3 + Vitest 3.2.6; full and production-only audits both report zero. See `docs/security-audit-npm.md`.
- Post-PR #76 audit (2026-07-31): the executable runner is merged on `main` at `748ff91`, but the read-only project audit found correctness and audit-integrity gates that must land before RUNNER-UI-001. The ordered follow-ups and evidence are in `handoffs/2026-07-31-pr76-post-merge-audit-v1.md`. `BUG-RESULT-CONTEXT-001` (order #1), `METRIC-002` (order #2), `PERSIST-AUDIT-001` (order #3), `RUNNER-OWNERSHIP-001` (order #4), `DATA-QUALITY-001` (order #5), `BUG-SWEEP-CONTEXT-001` (order #6), and `STRATEGY-VALIDATION-001` (order #7) are completed and recorded under Done. **All seven correctness/audit-integrity gates are closed**, and `RUNNER-UI-001` (order #8) has started: slice **a** (the typed, both-sides-pinned `discovery-event-v1` boundary) slice **b-1** (the pre-validated run-config envelope builder), and slice **b-2** (the runner panel, mock runner seam, and e2e) are all in Done, so **the merged backend runner is reachable from the product for the first time**. The Results Explorer UI landed as plan phase P01 (2026-09-16) and the runtime decoupling as P02 (2026-09-17); the next plan phase is P03 (cross-host ownership). Legacy-parity gaps remain split out as PARITY-001/002/003.
- PR CI runs typecheck / test / build / cargo-check (now incl. `cargo test`) — green per PR; `main` requires branches up to date before merge.
- Source-of-truth architecture: `STRATEGY_DISCOVERY.md` v3 and `README.md`.
- Historical context: `HISTORY.md` and `CONVERSATION_HISTORY.md`.

## Next

## In Progress

- **P09 authenticated external acceptance** (2026-09-21): implementation/local verification Done (below), but live Tiingo download, free entitlement and payment coverage still require the operator's token in Windows Credential Manager. User intends to register; native status currently `credential_missing`. Re-run the documented bounded range after setup and append evidence to the P09 handoff.

## Backlog

### AlphaBTC capability transfer and persistent service (2026-09-15)

User-directed scope: preserve AlphaBTC's useful research/data/accounting/operations capabilities in AlphaFactorForge, including a UI that controls a service able to run without the UI. Capability inventory C01–C20, source evidence, limitations, dependencies, and acceptance specifications are in [the transfer handoff](handoffs/2026-09-15-alphabtc-capability-transfer-v1.md). This extends the product requirements; it does not claim headless/24-hour AI automation is already implemented or silently complete existing Phase C/D work. This section is the status owner; the handoff is a specification, not a second task board.

Ordering: this section does not change the existing Phase B sequence. The Results Explorer UI (Done as plan phase P01, 2026-09-16) and the remaining Post-PR #76 order stay ahead; the plan-phase table below now sequences the ABC work (ABC-02a landed as P02; P03 is the next eligible phase).

Granularity: each ABC item is a bounded specification, not a one-session task. Before any ABC item moves to Next, split it into sub-items that each fit one session (AGENTS.md §9); the parent stays in Backlog as the status owner for its sub-items.

- [x] **ABC-01** — specify service/UI/CLI lifecycle, workspace ownership, recovery authority, and versioned command/event contracts. **Done across P00 (contract), P03a (ownership), and P03b (envelopes + ledger), 2026-09-17; `STRATEGY_DISCOVERY.md` §4 and README updated.** Original text: specify service/UI/CLI lifecycle, workspace ownership, recovery authority, and versioned command/event contracts. Recovery/migration authority follows the workspace lease holder, not the process type: the desktop still recovers when it starts in embedded mode (today's `main.rs` path, asserted by the PR #103 native smoke lane), and only skips recovery in connect mode. Must list the affected existing contracts (RUNNER-OWNERSHIP-001 OS-level guard retained; PR #103 smoke lane unchanged) and update `STRATEGY_DISCOVERY.md`/README on completion.
- [x] **ABC-02a** — extract the Tauri event sink (`TauriDiscoveryEventSink`/`AppHandle`) out of runner orchestration with zero behavior change; existing Rust tests prove event order and commit-then-emit are unchanged. Independently mergeable. **Done as plan phase P02 (2026-09-17)**; see the Done section.
- [ ] **ABC-02b** — add one bounded headless execution host using the existing computation/store; prove UI exit and competing-owner behavior.
- [ ] **ABC-03** — connect UI/CLI to the common service contract with idempotent commands and restart-safe state/event recovery; remove the desktop's connect-mode recovery path only (embedded mode retained; native smoke lane stays green).
- [ ] **ABC-04** — extend DATA-QUALITY-001/PARITY-002 with raw-response provenance, revisions, venue/quote/availability identity, and rejection fixtures. (Contract/storage, crypto, US ETF adapter code and TW ETF public-source acceptance are done across P06/P07/P09/P10. Still open only because P09 authenticated Tiingo entitlement/coverage/payment evidence awaits the operator token.)
- [x] **ABC-05** — freeze hypothesis/candidate artifacts before execution and preserve lineage and rejection evidence alongside existing run records. (P05, 2026-09-19: `hypotheses` / `research_attempts` / `research_artifacts`, `docs/research-history-v1.md`; the AI path that states failure modes is P15.)
- [ ] **ABC-06** — specify research feasibility, numerical precision, trial-family accounting, and adaptive-search/confirmation separation.
- [ ] **ABC-07** — extend the existing hidden Test task with atomic consumption, crash/restore invariants, and an overlapping-period disclosure ledger.
- [ ] **ABC-08** — produce execution/accounting differential fixtures before porting fill modes or changing metrics semantics.
- [ ] **ABC-09** — specify and implement archived engine artifacts and verified replay beyond contract hashes alone.
- [ ] **ABC-10** — extend Phase D with a bounded single-account forward-paper slice, shared ledger, idempotent checkpoint, and persistent risk latch.
- [ ] **ABC-11** — specify consistent workspace backup and isolated restore preserving artifacts, risk states, and Test consumption.
- [ ] **ABC-12** — extend Results Explorer/result-context work with service reconnection, provenance, stable selections, and persistent visible feedback; require real UI acceptance.
- [ ] **ABC-13** — specify durable scheduling, finite budgets, restart/sleep/update policies, and service health; split implementation/OS packaging after the contract.
- [ ] **ABC-14** — extend Phase C/full automation with a finite AI-batch contract covering executable DSL, information boundaries, generation evidence, budgets, and uncertain provider-call recovery.

Existing multi-asset/portfolio, supervised allocation reduction, and long-run operational validation remain separate follow-ups. The corresponding existing tasks below are retained; ABC work supplies additional contracts/acceptance rather than authorizing duplicate implementations.

#### Implementation plan phases (`docs/plans/active-plan.md`, 2026-09-16)

The maintainer-approved implementation plan sequences the ABC specifications (plus the AI-subscription, market-data, and paper work) into 23 bounded phases, P00–P22 (`docs/plans/active-plan.md` §5). **One phase per implementation session**; the next eligible phase is the first unfinished one whose dependencies are Done, and each phase needs its own explicit authorization. Phase rows below are the status owner for that phase; the ABC parent items above stay the specification owner and are not duplicated as tasks. Living capability status: `docs/autonomous-research-capability-registry.md`.

| Phase | Scope (module) | Depends on | ABC / existing task | Status |
| --- | --- | --- | --- | --- |
| P00 | Contract & compatibility precheck (runtime / AI / market contracts, local Codex and dependency check, capability registry) | — | ABC-01 contract half, ABC-14 contract half | **Done** (2026-09-16, see Done section) |
| P01 | Results Explorer over existing records/summaries/trades; no calculation change | P00 | "Build Results Explorer UI" (Phase B) | **Done** (2026-09-16, see Done section) |
| P02 | Runtime decoupling: runner event sink, DB path init, shared orchestration | P00 | ABC-02a | **Done** (2026-09-17, see Done section) |
| P03a | Cross-host ownership: OS lock, epoch, heartbeat, runner epoch checks, migration guard | P02 | ABC-01 implementation (ownership half) | **Done** (2026-09-17, including R1/R2 fencing regressions; Rust 179) |
| P03b | Idempotent command requests (`research-command-v1` requestId store) and the persistent event ledger (`research-event-v1` eventId, snapshot + cursor) | P03a | ABC-01 implementation (contract half), ABC-03 (part) | **Done** (2026-09-17, see Done section) |
| P04a | Headless service binary (`alpha-factor-forge-service`) and the loopback control API (`control-endpoint-v1`: manifest + token, `/v1/info`, `/v1/commands`, `/v1/events` long-poll, `/v1/shutdown` drain) | P03 | ABC-02b, ABC-03 (part) | **Done** (2026-09-17, see Done section) |
| P04b | Desktop connect mode: the desktop proxies through the published endpoint when another host owns the workspace, forwards ledger events to the window, the background hand-over (plan §3.1), and the P04 native smoke | P04a | ABC-02b, ABC-03 | **Done** (2026-09-18, see Done section) |
| P05 | Full research history: hypothesis, attempt, lineage, immutable artifacts | P03 | ABC-05 | **Done** (2026-09-19, see Done section) |
| P06 | Market foundation: instrument, calendar, raw, revision, snapshot | P03 | ABC-04 | **Done** (2026-09-20, see Done section) |
| P07 | Crypto adapter (Binance archive/REST) | P06 | ABC-04 | **Done** (2026-09-20, see Done section) |
| P08 | ETF market semantics (calendar, annualization, split, dividend, native currency, cost) | P06 | new | **Done** (2026-09-20; pure daily contracts, see Done) |
| P09 | US ETF adapter (Tiingo); authenticated acceptance needs the operator's account | P08 | new | **In Progress** — implementation/local validation Done (2026-09-21); authenticated external acceptance pending |
| P10 | TW ETF adapter (FinMind + TWSE) | P08 | new | **Done** (2026-09-21; five-symbol public-source smoke, see Done) |
| P11 | Executable DSL (TS + Rust evaluators, whitelist intersection) | P05 | Phase C "DSL validator" | **Done** (2026-09-22; see Done section) |
| P12 | Research feasibility & trial ledger | P05, P06, P11 | ABC-06 | Backlog |
| P13 | One-time Validation/Test reveal registry | P12 | ABC-07 | Backlog |
| P14 | Engine archive & workspace backup/restore | P03, P05, P13 | ABC-09, ABC-11 | Backlog |
| P15 | Codex subscription adapter — **AI unattended mode blocked until isolation is proven** (registry §2) | P00, P11, P12 | ABC-14, Phase C | Backlog |
| P16 | VS Code MCP adapter | P04, P05, P12 | new | Backlog |
| P17 | Persistent research scheduler | P07, P12, P13, P15 | ABC-13 | Backlog |
| P18 | Shared fill/accounting kernel | P08, P11 | ABC-08 | Backlog |
| P19 | Single-account paper | P14, P18 | ABC-10 | Backlog |
| P20 | Real paper scheduling, multi-account, auto-open authorization | P17, P19 | Phase D paper-live | Backlog |
| P21 | Integrated UX (onboarding, work center, notifications) | P01, P04, P17, P20 | ABC-12 | Backlog |
| P22 | Windows operational acceptance (7-day soak) | P14, P20, P21 | new | Backlog |

Ordering note: with this plan the maintainer has explicitly reordered — P01 (Results Explorer) was the next Phase B step and is Done; P02 (ABC-02a) follows without waiting for the ABC-01 implementation half, which is P03.

### Post-PR #76 project audit follow-ups (2026-07-31)

The detailed evidence, shortest reproductions, contract cautions, and per-task acceptance plan live in `handoffs/2026-07-31-pr76-post-merge-audit-v1.md`. The order below is deliberate. Promote and complete one small task at a time; do not bundle these findings into RUNNER-UI-001 or rewrite the merged PR #76 scope.

#### Correctness and audit-integrity gates before RUNNER-UI-001

- **METRIC-002 (P1)** — completed locally; see the Done section.
- **PERSIST-AUDIT-001 (P1)** — completed in PR #89; see the Done section.
- **RUNNER-OWNERSHIP-001 (P1)** — completed in PR #90; see the Done section.
- **RUNNER-UI-001b-1 / b-2** — completed on `feat/discovery-run-config` and `feat/discovery-runner-panel`; see the Done section. The rich Results Explorer remains the separate Phase B task below.
  - **RUNNER-UI-001a** — completed on `feat/runner-event-contract`; see the Done section. It unblocked itself: every gate it waited on (BUG-RESULT-CONTEXT-001, METRIC-002, PERSIST-AUDIT-001, RUNNER-OWNERSHIP-001, DATA-QUALITY-001, BUG-SWEEP-CONTEXT-001, STRATEGY-VALIDATION-001) is in Done. When promoted, replace the stale frontend event DTOs with the backend `discovery-event-v1` contract, add the active-run wrapper, and make the throttle cancelable and stale-timer-safe.

#### High-priority input and anti-overfitting guards

- **DATA-QUALITY-001 (P1)** — completed in PR #94; see the Done section.
- **BUG-SWEEP-CONTEXT-001 (P1)** — completed on `fix/sweep-result-context`; see the Done section.
- **STRATEGY-VALIDATION-001 (P1)** — completed on `fix/strategy-period-validation`; see the Done section.

#### Persistence, I/O, CI, and test fidelity

- **PERSIST-INVARIANT-001 (P2)** — completed on `fix/persist-bundle-invariants`; see the Done section.
- **IO-ROBUSTNESS-001 (P2)** — completed on `fix/atomic-report-filenames`; see the Done section.
- [ ] **DB-ASYNC-001 (P2)** — move large dataset import, result persistence, and filesystem writes behind async Tauri commands plus `spawn_blocking`; retain the single SQLite coordinator boundary and add a controlled slow-operation responsiveness test before considering pagination/batching.
- [ ] **CI-TAURI-SMOKE-001b (P2)** — script one real invoke/event round trip through the native bridge. Blocked on a **maintainer dependency decision**: it needs a WebDriver stack (`tauri-driver` + `msedgedriver`), which the no-new-dependencies rule reserves for explicit approval. Slice a (below) already builds and starts the binary, so this is the remaining half. Spec: `docs/improvement-backlog.md` → CI-TAURI-SMOKE-001.
  - **CI-TAURI-SMOKE-001a** — completed on `feat/native-tauri-smoke-lane`; see the Done section.
- [ ] **TEST-MOCK-PARITY-001 (P2)** — make mock `saveStrategy` reproduce SQLite same-hash UPSERT identity/name/source semantics and add a regression proving repeated saves keep one stable row/id.
- [ ] **TEST-E2E-LAYOUT-001 (P3)** — harden the chart e2e specs that derive mouse coordinates from a `boundingBox()` taken right after load: `pan.spec.ts:11` and `zoom.spec.ts` (replay case). The skin layer loads its web fonts from a CDN, so on a cold runner they swap in after first paint and reflow the rows above the chart (measured ~4px down on CI's Ubuntu fallback, ~6px up on Windows); a swap landing between the box read and the drag can put the pointer off the canvas. Found while fixing the wheel-zoom case in PR #81, which no longer asserts on position at all — these two only compute coordinates, so they are latent, not failing. Wait for the layout to settle before reading the box; do not weaken what the specs assert.

#### Performance, documentation, and tooling debt

- [ ] **PERF-001 (P2)** — execute the existing `docs/improvement-backlog.md` plan to move the maximum-256-combination parameter sweep into the Web Worker with job id, cancellation, stale-result protection, and sync/worker determinism coverage; do not create a duplicate performance specification.
- [ ] **PERF-CHART-COMPUTE-001 (P2)** — memoize full-series indicators and the trade map independently of hover/replay repaint, then throttle pointer rendering with `requestAnimationFrame`; benchmark a large dataset before and after.
- [ ] **PERF-CHART-BRIDGE-001 (P2)** — split native chart-window dataset transfer from lightweight view-state updates so strategy/overlay/trade changes do not repeatedly serialize all candles; retain the ready-handshake and targeted-event permissions.
- [ ] **DOC-STATE-002 (P2)** — reconcile root/local READMEs, `alpha-factor-forge/TODO.md`, and `PHASE_A_VERIFY.md` with migrations 0001–0003 and the completed backend runner while keeping `tasks.md` as the sole status source. Documentation-only PR; do not edit contracts to claim unimplemented audit fixes.
- [ ] **TOOLCHAIN-001 (P3)** — pin the supported Rust toolchain in repository/CI and either prove the declared MSRV in a lane or update the claim; keep the native Tauri dependency floor explicit.
- [ ] **CI-RUSTFMT-001 (P3)** — format the existing `db_commands.rs` drift and add repo-wide `cargo fmt --all -- --check` to CI without mixing unrelated Rust cleanup.
- [ ] **DB-MIGRATION-DIAGNOSTIC-001 (P3)** — replace migration existence-query `unwrap_or(false)` with `OptionalExtension`-style not-found handling while propagating real SQLite errors; add the diagnostic regression without changing append-only migration semantics.
- [ ] **SEC-RUST-001 (P3)** — add a pinned Rust advisory scan to CI or document the chosen equivalent; the 2026-07-31 audit could not run `cargo audit` because it was not installed.

#### Accepted contracts requiring an explicit decision before code changes

- [ ] **INTERVAL-CONTRACT-001** — decide whether unknown/alias intervals remain the intentionally tested daily fallback or become a strict canonical interval set. If changed, version the contract, update TS/Rust parity fixtures and annualization tests, and record the behavior change in `CHANGELOG.md`; do not fold this decision silently into DATA-QUALITY-001.
- [ ] **DECISION-ZERO-TRADE-001** — decide whether a zero-trade candidate should continue to fail Random Entry/run execution under `random-entry-v1`, or become a persisted Gate rejection with explicit insufficient-evidence status. Any implementation requires coordinated Random Entry/Gate/record contract versioning and mixed/all-zero runner tests.

### Legacy-parity gaps (split out of the UI port umbrella, 2026-07-30)

These were named inside the UI port entry and must not be buried by closing it. They stay in Backlog — naming a deferred gap does not schedule it, and none of them blocks the runner slices. Promoting any of them to Next is a product decision.

- [ ] **PARITY-001** — core STOCH indicator, then the `stoch*` params signals and blocks operands that have been deferred since Slice 1/4a.
- [ ] **PARITY-002** — backend candle fetch command. The webview CSP is `default-src 'self'`, so live exchange `fetch` cannot come from the frontend; today data arrives only via file/JSON import or the seeded sample seam.
- [ ] **PARITY-003** — strategy delete in the library (left optional/deferred by Slice 7-3).

### Phase B: Discovery And Validation

- [ ] Implement Phase B validation foundations
  - [x] VAL-001: lock the pure deterministic 60/20/20 bar split and equal-embargo range contract.
  - [x] VAL-002: run the split plan through the backtest pipeline (Train + Validation segments only; Test never executed).
  - [x] VAL-003: usage-aware embargo derivation (max used-signal lookback + explicit holding allowance, recordable breakdown).
  - Keep Test hidden from ranking and prompts (holds so far: nothing runs or reads the Test segment).
  - Add walk-forward only after the basic split is stable.

- [ ] Implement Gate + Score and benchmarks
  - Gate: minimum trades, cost-adjusted average trade, rolling-window consistency, max drawdown, concentration limits, benchmark wins.
  - [x] GATE-001: §5.1 hard elimination gate as a pure judgment with explicit thresholds and fail-closed evidence.
  - [x] SCORE-001: §5.2 score-v1 ranking breakdown per the PR #61 handoff Resolution (regime component deferred to REGIME-001; Gate→Score orchestration remains with the runner).
  - Score: OOS CAGR, Sortino, Calmar, regime robustness, profit factor, consistency, complexity/turnover/data-mining penalties.
  - Benchmarks: Buy & Hold, SMA, RSI, Bollinger, Random Entry.
  - [x] BENCH-001: the four deterministic benchmarks (Buy & Hold, SMA 50/200, RSI 14 30/70, Bollinger 20/2) as a pure suite.
  - [x] BENCH-002: seeded Random Entry Monte Carlo with matched trade count + holding periods; returns the distribution + candidate percentile (no pass/fail — Gate owns the threshold).

- [ ] Add duplicate skip and result reuse
  - Use `strategy_hash`, `dataset_hash`, and segment.
  - Never retest the same strategy/data/segment combination unnecessarily.

- [x] Implement the Tauri backend discovery job runner (completed by RUNNER-CONFIG-001, RUNNER-STORE-001, and RUNNER-EXEC-001; detailed entries are under Done)
  - Support start, pause, resume, cancel, checkpoint, and progress events.
  - Keep heavy discovery off the UI thread and out of the Web Worker.
  - Persist run/job progress in SQLite.

- [x] Build Results Explorer UI — completed as plan phase P01 (2026-09-16); see the Done section.
  - [x] Show Validation ranking only by default (gate-passed by persisted score, then gate-failed, newest first).
  - [x] Filters (strategy / dataset / gate), record detail (contracts, fingerprints, embargo + Train/Validation ranges, all eight Gate criteria, Score components/penalties, benchmark net returns + Random Entry), latest Train/Validation summaries, and on-demand trades.
  - [x] Keep Test hidden: `test` segment rows are filtered by the reader and the split plan's Test range is never rendered.
  - Deferred (not in P01): DSL tree inspection (no executable DSL yet — P11), benchmark *deltas* beyond the persisted net returns, and service reconnection / provenance / stable-selection extensions (ABC-12 / P21).

- [ ] Implement minimum strategy lifecycle
  - `candidate -> validated -> rejected`.
  - Defer `paper_live`, `promoted`, and `quarantined` automation to Phase D.

### Phase C: Minimal AI Strategy Lab

- [ ] Implement secure AI key storage
  - Store AI API keys only through OS keychain/secure storage.
  - Frontend may set/check/delete key status but must never read key values back.

- [ ] Add backend AI connection test
  - Route all AI calls through Tauri backend commands.
  - Handle rate limits, retries, and quota errors in backend.

- [ ] Implement JSON Strategy DSL generation and validation
  - AI may output JSON DSL only.
  - Validate via whitelist schema and suspicious-token checks.
  - Mirror validator behavior in Rust for defense in depth.

- [ ] Add manual approval before AI strategies enter the queue
  - Save prompt/raw/parsed/validation records in `ai_generations`.
  - Approved strategies become `strategy_def(source=ai, type=ai_dsl)`.

### Deferred / Optional Product Work

- [ ] Walk-forward analysis beyond the initial split.
- [ ] Multi-asset portfolio backtesting. (C08 in the AlphaBTC transfer handoff informs it; not an ABC item — see that section's closing note.)
- [ ] Alerts and webhooks.
- [ ] Paper-live forward test flow. (Specification: ABC-10.)
- [ ] Hidden Test one-time reveal and promotion flow. (Specification: ABC-07.)
- [ ] Strategy clustering and family refinement.
- [ ] Meme/low-liquidity risk filters and dynamic slippage.
- [ ] Full closed-loop AI automation. (Specification: ABC-14.)

## Done

- [x] **P11 — Executable DSL** (2026-09-22, branch `feat/p11-executable-dsl`). Added the versioned, data-only `strategy-dsl-v1` contract and matching pure TypeScript/Rust evaluators. The executable indicator whitelist is the explicit single-series intersection already implemented by both cores (EMA/SMA/WMA/RSI/ROC/ATR/STDDEV/HIGHEST/LOWEST plus raw OHLC/HLC3); ambiguous multi-output MACD/BBANDS remain excluded. Validation covers exact fields, operator arity and numeric/boolean types, finite values, parameter references, indicator periods in integer `[2,400]`, temporal offsets in integer `[1,400]`, causal SHIFT/RISING/FALLING, depth 8, nodes 64, and code/IO/network/import token rejection. A hand-authored `strategy-dsl-v1.json` fixture makes both languages assert the same accepted signals, analysis metadata, and illegal matrix across every v1 operator/indicator family, with each valid entry/exit producing both booleans. `discovery-config-v2` pins the DSL and enumeration versions, requires a fixed DSL base with no axes, keeps cost/risk/sizing/execution outside the tree, rejects invalid DSL before enumeration or writes, and revalidates DSL plus holding allowance at execution. The existing runner then derives embargo from the DSL, applies the existing Train/Validation/benchmark/Gate/Score path, stores `strategy_def.type=dsl` plus DSL/param schema, and freezes the P05 hypothesis/attempt lineage in the enqueue transaction. The typed Tauri validation command now returns the structured report; generation/provider/approve remains P15. No migration/dependency/UI change. Validation: 973 Vitest, 373 Rust, 78 Playwright; typecheck, build and locked all-target check pass; clippy only the 5 pre-existing warnings. Contract: `docs/strategy-dsl-contract.md`.

- [x] **P10 — TW ETF adapter (FinMind + TWSE)** (2026-09-21, `feat/p10-finmind-tw-etf-adapter` from merged main `373bba1`): strict `tw-etf-settings-v1`, bounded service `fetch-tw-etf`, FinMind `TaiwanStockPrice` raw OHLCV and trading/event feeds, independent official TWSE monthly-quote reconciliation with ROC-date conversion and share-volume preservation, explicit 2025 trading calendar/listings/dividend payment/split/suspension evidence for 0050/006208/0056/00878/00713, and P06 immutable provenance/revision/report/snapshot storage. Real 2025-06-09..21 smoke produced 0050 5/5 and the other four 10/10 requested bars; 0050's 4:1 split/suspension and 00713's dividend/payment path passed. Costs remain explicitly unconfirmed, so all five snapshots are degraded and cannot qualify. 13 added Rust tests; **366 Rust / 969 Vitest / 78 Playwright**, typecheck/build/check pass, clippy only the 5 existing warnings. No migration, dependency, strategy-engine or UI change. Updated active plan, roadmap, task/status, README, architecture/market contracts, capability registry and changelog. [Acceptance evidence and limits](handoffs/2026-09-21-p10-tw-etf-v1.md).

- [x] **P09 implementation and local validation** (2026-09-21, `feat/p09-tiingo-etf-adapter` from main `4e48343`): service CLI, strict non-secret settings, OS credential reader, raw/adjusted EOD and company-event originals, immutable revisions, per-symbol eligibility and missing permission/payment/cost blockers. 16 added Rust tests cover normal/rejection/revision and all five defaults; total 353 Rust / 969 Vitest / 78 Playwright, typecheck/build/check pass. No migration, no core changes; only direct-pins already-locked Windows credential API dependency. README/roadmap/technical contracts/changelog/active-plan synced. **This completes code/local validation, not P09 live acceptance**; that remains In Progress above. [Evidence, limitations and next acceptance steps](handoffs/2026-09-21-p09-tiingo-v1.md).

- [x] **P08 — ETF daily market semantics** (2026-09-20): followed active-plan's one-phase limit and verified its prerequisites on current main `bfb4216`. Reuses P06 session calendars and existing metrics through new opt-in TS/Rust contracts. Dividends accrue without spendable cash until payment; splits preserve total basis/equity and adjust pending quantities/prices; raw prices stay unchanged while point-in-time signal views reject future/unobserved events. Explicit native currency and cost confirmation, bounded calendar/action evidence, degraded missing payment/completeness, elapsed-time CAGR and frozen session-frequency volatility. Shared 55-case parity matrix, non-finite regressions, legacy CAGR lock; 969 Vitest / 337 Rust / 78 Playwright pass. No migration or dependency. Updated plan execution note, roadmaps/status, README, market/architecture/parity docs and changelog. [Acceptance evidence and remaining integration boundaries](handoffs/2026-09-20-p08-etf-semantics-v1.md). P09/P10 and P18 not started.

- [x] **P07 acceptance fixes** (2026-09-20): completed Claude's partial fixes, added legacy-cache and snapshot-read quarantine, recorded each response after arrival, preserved retry failures, and verified CHECKSUM evidence on cache hits. Seven Rust regressions cover the two findings and their upgrade/rejection paths; all 335 Rust and 911 Vitest tests pass, build/check pass, no new clippy warnings. Existing isolated-workspace data refetched with corrected evidence and then reused successfully (96/96 both times). See [Resolution](handoffs/2026-09-20-pr109-p07-acceptance-review-v1.md#resolution). P08 not started.

- [x] **P07 acceptance review** (2026-09-20, PR #109 at `6162889`): independently passed 911 Vitest / 328 Rust / 78 Playwright plus build/check, and repeated all five documented real retrieval ranges. **Acceptance withheld pending two fixes**: archive availability can incorrectly qualify old forward-observed snapshots; published CHECKSUM response bytes are not retained. Two failing counterexamples were reproduced only in an isolated HEAD copy; production implementation unchanged. See [review and required fixes](handoffs/2026-09-20-pr109-p07-acceptance-review-v1.md). P08 not started.

- [x] **P07 — Crypto adapter (Binance archive + REST)** (2026-09-20, branch `feat/p07-binance-adapter` from main `503fc99`). Plan phase P07 / ABC-04 (the source half): the first real market data this workspace has ever retrieved. `market/http.rs` is the only module that reaches the network (HTTPS to allowlisted hosts, redirects refused because a redirect changes the source, size cap, bounded retries for transport/429/5xx only), and everything above it works against an `HttpFetcher` trait, so the whole ingest path is tested offline. `market/sources/binance.rs` reads what Binance actually publishes: the published SHA-256 CHECKSUM (which must name the same file), a single-entry ZIP verified by entry count, declared size and CRC-32, the 12-column kline CSV, and the REST JSON — with the **time-unit rule** the contract warned about: the unit is resolved per FILE, a file whose rows disagree is refused (`time_unit_unresolved`), an open time that is not a whole millisecond is refused rather than rounded (`time_unit_not_exact`), and the resolved unit is recorded in that file's provenance. `market/ingest.rs` plans the units a range needs (monthly → daily when the month is unpublished → the same exchange's REST for the closed tail), reuses stored bytes instead of re-asking, keeps every rejection **with the bytes that failed**, refuses rather than resolves a disagreement between two units, drops bars that had not closed at the cut, imports through the unchanged `import_dataset_with_candles`, and hands the result to P06. `alpha-factor-forge-service fetch --instrument … --interval … --from … --to …` runs one retrieval as the workspace owner (exit 5 = retrieved, but the range is not usable). Two findings came out of running it for real: **(1)** a snapshot's coverage answers for the dataset's OWN bounds, so a retrieval that stopped early looked complete — the ingest now audits the **requested range** separately (`IngestReport.rangeCoverage`, events tagged `scope: "requested-range"`, outcome `rangeIncomplete`); **(2)** the archive and the same exchange's REST endpoint were refused as a source conflict, so the pure contract gained `sourceOrigin`/`sourcesShareOrigin` (`<origin>[-<endpoint>]`) and snapshot composition tolerates a `source_mismatch` only between two endpoints of one publisher — two exchanges are still never one series. Dependencies added and locked in this phase: `ureq 3.4.2` (rustls + ring + bundled webpki roots, no system TLS) and `flate2 1.1`; 13 crates entered the lock. **Real runs** (isolated workspace, 2026-09-20): BTCUSDT and ETHUSDT 1h for all of 2025 = **8760/8760 bars each**, snapshot eligible; BTCUSDT 2024-12-30→2025-01-03 crosses the archive's millisecond→microsecond change inside one range (**96/96**); BTCUSDT 2026-09-01→21 = 19 daily archives + one REST page (**462/463**, one bar not due yet); BTCUSDT 2017-07-01→09-01 reports the pre-listing gap **2017-07-01 00:00 → 2017-08-17 03:00 (1132 bars)** and refuses the range. **No claim of multi-year completeness** is made beyond those runs. Tests (+26 Rust 302 → **328**, +2 Vitest 909 → **911**): 8 source-parsing tests against four committed real archive files (both eras), 9 ingest tests driven by a fake fetcher, 3 URL/retry policy tests, 3 fetch-option tests, 1 CLI test, 1 snapshot composition test, plus the origin rows in both runtimes' parity suites. Adapter contract: `docs/market-source-binance-v1.md`. Not in scope: Coinbase comparison source, `exchangeInfo` trading specifications (instruments stay paper-ineligible), detecting archive revisions, scheduling (P17), UI (P21).
- [x] **P06 acceptance fixes** (2026-09-20). Required provenance for every snapshot kind; forward-observed availability checked against the cut with exact instant comparison; full stored OHLCV identity and plausibility verified before both creation and Existing returns; identical revision retries resolved before fork rejection. Five new Rust tests cover all four review findings, metadata/payload corruption without writes, cutoff boundaries, and late revision retries. Rust **302**, Vitest **909**, typecheck/build/check passed; no new clippy warnings. See [review Resolution](handoffs/2026-09-20-p06-acceptance-review-v1.md#resolution).
- [x] **P06 — Market data foundation** (2026-09-20, branch `feat/p06-market-foundation` from main `a0f738e`). Plan phase P06 / ABC-04 (contract + storage half): the shapes every market adapter will write into, with no network access of any kind. **Pure contract, mirrored in both runtimes** (`src/core/market-data/foundation.ts` ↔ `src-tauri/src/discovery_core/market_foundation.rs`, pinned by `fixtures/rs-core/market-foundation-v1.json`, authored from the specification rather than recorded from either implementation): `market-interval-v1` (strict cadence, unknown interval has none — no daily fallback), instrument-id parsing (`<market>:<venue>:<symbol>`, `USDT` never renamed `USD`), time-unit detection over the four bands derived from `market-data-quality-v1`'s own window (a batch that switches milliseconds → microseconds mid-way reports the index, and nothing converts), session calendars (`continuous` / `trading-days`, version in the id, no timezone database: a daily bar is the trading date's UTC midnight), expected-bar derivation (weekends, holidays, halts, and pre-listing time are not gaps; six refusal issues instead of a guess), the coverage audit (`out_of_order`, `duplicate_timestamp`, `unaligned_timestamp`, `unexpected_bar`, `missing_bar` merged into contiguous ranges, `unclosed_bar` — all blocking, each with an action code, ordered by `(rangeStart, declared code rank)`), and series-combination conflicts (a second venue, quote, source, or basis is never one series; a `comparison` source is evidence, never a component). **Storage** (`migration 0008_market_foundation`, `src-tauri/src/market/`): six append-only tables with UPDATE/DELETE triggers — `market_calendars` (a version is written once; the same id with different content is refused), `market_instruments` (content-hashed revision chain; registering one requires its calendar, which is the honest blocking point for `nyse-v1`/`twse-v1`), `market_provenance` (raw bytes in the artifact store by checksum, rejected retrievals kept for replay, `availableAt ≤ retrievedAt`, a linear non-forking revision chain), `market_snapshots` + `market_snapshot_sources` (built only when nothing blocks; freezes the instrument revision, calendar version, price basis, and sources; `degraded` when corporate actions or costs are unconfirmed; `qualification_eligible = ok && not demo`), and `market_quality_events` (codes and actions outside the contract are refused). `ArtifactStore::put_with_extension` keeps a raw CSV/ZIP under its own extension. `runtime::open_workspace` registers `crypto-24x7-v1` after migrating (owner-only, idempotent). **Nothing existing changed**: 0001–0007, `dataset-content-v2`, `market-data-quality-v1`, and `barsPerYear` are untouched, no dependency was added, and a dataset with no snapshot is legacy — it imports and backtests exactly as before and simply cannot acquire new qualification. Tests (+36 Rust 261 → **297**, +30 Vitest 879 → **909**): 8 Rust + 21 vitest unit tests on the pure contract, 7 Rust + 9 vitest parity tests over the shared fixture, 19 Rust storage tests (calendar immutability, revision chains, rejected/superseded/second-venue components, a gap that names its bars and its action, an ETF holiday that is not a gap, degraded defaults, forward-observed availability, legacy datasets). Shapes and invariants: `docs/market-foundation-v1.md`. Not in scope: downloads and CHECKSUM verification (P07/P09/P10), real NYSE/TWSE holiday data (P09/P10), ETF corporate-action and annualisation semantics (P08), command surface and UI (P16/P21), binding research to snapshots (P12+).
- [x] **P05 acceptance follow-up** (2026-09-20, PR #107) — closed both Codex findings. Pre-0007 paused runs now reconstruct and freeze lineage for still-queued candidates inside the same transaction that resumes the run; existing attempts must match candidate identity plus input/engine fingerprints, and production claim/commit require exactly one attempt or roll back jobs, projections, validation records, and artifact references. The research-history panel uses a request generation so a slow older artifact read cannot replace the latest selection. Two Rust regressions and one reverse-order Playwright regression mutation-lock the fixes. Validation: Rust **261** (52＋207＋2), Vitest **879**, typecheck/build/cargo check clean, Playwright **78/78**. Resolution: `handoffs/2026-09-19-p05-research-history-v1.md`.
- [x] **P05 — Full research history** (2026-09-19, branch `feat/p05-research-history` on main after PR #106). Plan phase P05 / ABC-05: migration `0007_research_history` adds three append-only tables beside the existing run records — `hypotheses` (frozen before execution, identity = SHA-256 of the canonical content so a repeated registration is the same row, UPDATE/DELETE refused by triggers), `research_attempts` (one per candidate, `attempt_key = run:<run>:candidate:<index>`, frozen input/engine fingerprints, status `submitted → running → completed | failed | skipped` with terminal rows and frozen columns guarded by a trigger), and `research_artifacts` (content-addressed references, never deleted). The runner writes every transition inside the SAME transaction as the job transition it mirrors: the hypothesis and all attempts land with the enqueue (`start_discovery_run_with_lineage`), claim → running, commit → artifact row + completed, run failure → failed with the reason, cancel → skipped with the reason, crash recovery → running back to submitted (the same attempt). `research::artifacts::ArtifactStore` (`<workspace>/artifacts/<sha[0..2]>/<sha>.json`): stage → fsync → verify → atomic rename → only then the DB reference, checksum-verified on read, no delete operation, unreferenced files listable. `candidate-result-v1` holds the complete result (both summaries, all trades, the validation record, the digest) in canonical JSON; the existing `backtest_summary`/`backtest_trades` stay the LATEST projection (still upserted). Desktop commands `list_research_attempts`, `get_research_attempt` (attempt + hypothesis + verified result), `list_hypotheses`, `list_unreferenced_artifacts`; typed `research.*` client; a read-only "研究歷史" panel under the Results Explorer (mock-driven in `?mock=1`). Tests (+7 Rust 246 → 253 incl. the other agent's, +2 Playwright 77): a run freezes its hypothesis and attempts with the enqueue and every completed attempt has a verified artifact with no unreferenced file left; a re-run reuses the projection row but keeps the earlier attempt and its artifact byte-for-byte while old/new projections match their artifacts; failed and cancelled attempts keep their reasons and every edit/delete is refused; hypothesis dedup and validation; recovery requeues the same attempt; artifact put/read/tamper/unreferenced; canonical JSON; e2e re-run adds attempts and cancel skips with the reason. Not in scope: hypotheses with stated failure modes (P15), attempt lineage between candidates (P11/P15), export/backup (P14), envelope exposure of the reads (P16), full-text experience search (P17).
- [x] GitHub publication catch-up (2026-09-19): committed outstanding P04b H2 fixes and existing P03b notes; preserved original history, rebased onto latest main, published four previously local branches and created draft PR #106. All local branch commits are backed by origin; no remote history overwritten. See `handoffs/2026-09-19-github-sync-v1.md`.

- [x] P04b H2 startup-race follow-up (2026-09-19): stop assuming the first ledger page is covered by the window snapshot; conservatively request an initial re-read, register all panel listeners before the initial snapshot, serialize refreshes and repeat a read invalidated by a notification. One real-runner Rust regression and two deterministic Playwright regressions fail on the old code and pass with the fix. Rust 247 / Vitest 879 / Playwright 75 verified (74 full-suite passes + one navigation timeout passing on focused rerun). Contract §1.5, changelog and handoff Resolution updated; P05 not started.

- [x] P04b acceptance follow-up (2026-09-18): H1 — a take-back whose `stop` is refused keeps the forwarder (the desktop stays connected AND following; only a successful stop detaches it). H2 — the bridge now applies contract §3 itself: a NEW ledger-gap marker, a state version that moved with no row, a row the window could not be handed, or a reconnect posts `runtime://resnapshot` (once per fact), and the panel re-reads the active run or — when the followed run has ended — that run's own progress, so a missing Done row can no longer leave a stale "running". M1 — losing the service (any ledger read failure, including a 401 on a reused port) starts rediscovery: the endpoint files are re-read and re-verified for THIS workspace (instance, protocol, schema), another workspace's endpoint is refused, the host's proxy is replaced on success and the window is told to re-read; the cursor survives because ledger ids never restart. Three regressions in `discovery_runner/tests/host.rs` mirror the reviewer's probes and each fails with its fix reverted; the gap-recovery UI path has a Playwright test (`discoveryDropDone=1`) that fails without the panel's handler. Rust 246 / vitest 879 / Playwright 73.
- [x] **P04b — Desktop connect mode and background hand-over** (2026-09-18, branch `docs/p00-contract-precheck`, on top of the reviewed P04a). Second half of plan phase P04; with it ABC-02b and the P04 row are complete. The desktop is now one of two hosts (`runtime::host::HostMode`): **embedded** (owns the workspace as before) or **connected** (a background service owns it; the desktop proxies). Startup (`host::open_or_connect`): own the workspace when it is free; on `NotOwner` read the endpoint manifest, verify `/v1/info` (instance id, workspace id, protocol version), open the database WITHOUT migrating (`db::open_migrated`: exactly this build's schema — pending → `SchemaPending`, unknown → `SchemaTooNew`), and connect; an owner that publishes nothing still stops startup with the reason. In connect mode every discovery command (`start/pause/resume/cancel/progress/active`) and `dispatch_research_command` go to the service as `research-command-v1` envelopes (`runtime::connect::ServiceProxy`, fresh CSPRNG request ids for the legacy commands, the caller's own id for envelopes), repository commands use the non-owner connection, `run_migrations` refuses, and a forwarder thread long-polls `GET /v1/events` from the ledger's end at connect time and re-emits each row to the window under its stored channel (`discovery://progress|result|done`) — the window cannot tell the modes apart. `runtime://host` (`{hostMode, serviceReachable, reason}`) is posted when the mode changes or the service stops/resumes answering, and the panel re-reads the active run on every announcement (contract §3 reconnect). Plan §3.1 background mode (`host::hand_over_to_service`): close admission and drain in-flight embedded commands (`Admission` guard held by every mutating command) → ask every coordinator to checkpoint and wait → release the lease → launch `alpha-factor-forge-service` beside the desktop binary, detached, logging to `<workspace>/service.log` → wait for its endpoint → connect and follow; on failure the desktop re-owns the workspace (or reports `switching` with the reason). The reverse (`take_back_from_service`): `service stop` (drain to checkpoint), relock with a short retry, embedded again. Tauri: `get_host_status`, `enter_background_mode`, `exit_background_mode`; `get_workspace_info` gains `hostMode`; `AFF_DATA_DIR` points both binaries at an isolated workspace for native smokes. UI: a host badge (`host-mode`, `data-host-mode`) and one button (`host-mode-toggle`: 在背景繼續 / 收回桌面) in the discovery panel header; the `?mock=1` client simulates both modes (`hostMode=desktop-connect`, `hostSwitchFail=1`). Tests (+7 Rust 236 → 243, +3 vitest 877, +3 Playwright 72): `discovery_runner/tests/host.rs` (own-or-connect over a real file workspace incl. a lost service reported once; a hand-over with an in-flight run waits for the checkpoint, the service finds a PAUSED run — no orphan — the desktop resumes it through the proxy, sees Done forwarded, and takes it back at epoch 3; a hand-over blocks on an admitted command and a failed launch re-owns at epoch 2; take-back after the service died; the REAL service binary launched detached and stopped by the take-back), the forwarder's order/once guarantee over the API, `open_migrated`, the host-event parser, and `e2e/host-mode.spec.ts`. Native smoke on an isolated workspace with the real binaries: service first → desktop logs "connected to the background service"; desktop first → service exits 2. Not in scope: Windows ACLs, a signal handler, bundling the service into the installer, an in-window re-snapshot on `ledgerGap` (P21), and a UI-driven native hand-over run (needs the dev server + a hand).
- [x] P04a acceptance follow-up (2026-09-18): fixed shutdown admission/drain race, ineffective pause deadline, stale identity reported live by `status`, and fragmented HTTP heads bypassing the 16 KiB cap. Each regression failed before its fix. Shutdown retains ownership after a drain timeout and releases only after admitted mutations and coordinators exit; contract §4.1 updated. Rust 236 / Vitest 874, typecheck/build/check pass; clippy only existing core warnings. P04b remains Next eligible; this turn handled the user's requested review-and-fix branch. Handoff: `handoffs/2026-09-17-p04a-headless-service-v1.md`.

- [x] **P04a — Headless service binary and loopback control API** (2026-09-17, branch `docs/p00-contract-precheck`, on top of the reviewed P03b). First half of plan phase P04, split like P03a/P03b so each half is one session; the desktop is untouched (connect mode is P04b). A second binary in the same Cargo package, `alpha-factor-forge-service` (`src/service_main.rs`, `default-run` stays the desktop, its unit-test target is off so the shared modules' tests run once), compiles the same `db`/`discovery_runner`/`runtime` modules as the desktop and never names Tauri (`runtime::boundary_tests` now guards the new files too). `runtime::service`: `run` owns the workspace as host kind `service` through the same `open_workspace` (OS lock → migrate → epoch → recovery → heartbeat), builds the P03b `Dispatcher` over a sink that only wakes long-poll readers, binds `127.0.0.1:0`, publishes `control-endpoint.json` + `control-token` (staged and renamed into place; 0600 on Unix, profile ACL on Windows) in the workspace directory, serves until `POST /v1/shutdown` (or lost ownership), then drains — refuses mutating commands, pauses every live coordinator and waits for its checkpoint to commit under this epoch — withdraws the endpoint while still holding the lock, and releases; `stop` verifies the manifest against `/v1/info` (instance id) and waits for the port to go away; `status` prints the manifest and what answers; `--data-dir` overrides the default `<platform data dir>/com.alphafactorforge.desktop` (`dirs::data_dir`, exactly tauri's `app_data_dir`; the identifier is pinned to `tauri.conf.json` by a test). `runtime::control_api` (std-only HTTP/1.1, one request per connection, `Connection: close`): every request needs a loopback `Host` (ours, port checked), no `Origin`, and `Authorization: Bearer <token>` compared in constant time — failing those answers 400/403/401 before any route; bodies need `Content-Length` (≤ 1 MiB, no chunked), heads ≤ 16 KiB; `GET /v1/info`, `POST /v1/commands` (the envelope, answered `{result}` or `{error: CommandError}` under a status that matches the code), `GET /v1/events?afterEventId&limit&waitMs` (the `events.read` page, parked up to 30 s until the ledger grows, the state version moves, or a shutdown begins), `POST /v1/shutdown` (202; reads keep answering during the drain). Long-poll was chosen over SSE (contract §4). Dependencies `dirs = "6"` and `getrandom = "0.3"` were already in `Cargo.lock` through tauri (no new crate; the token is 32 CSPRNG bytes). Tests (+27 Rust, 205 → 232): 23 unit tests (token, manifest, head parser, host rule, every refusal over real sockets, long-poll wake, shutdown, stop, CLI, in-process lifecycle, identifier pin), 2 runner-level tests over the API (a run started by a client that then disappears keeps going, a new client adopts it via `discovery.active` and follows the ledger live; a drain pauses at a checkpoint the next owner resumes with no orphan), and `tests/service_smoke.rs` spawning the REAL binary (endpoint published, 401 without token, envelope answered, second service exits 2, `status`/`stop` exit codes, endpoint withdrawn, next owner gets epoch 2, token never logged). Manual: curl against the binary (401/200/403 Origin/localhost 200/POST/long-poll 1 s/stop). Not in scope: the desktop's connect mode and hand-over, event forwarding to the window, bundling the service into the installer, a signal handler (Ctrl+C is the crash path recovery already covers), and an explicit Windows ACL call.
- [x] **P03b — Command envelope and event ledger** (2026-09-17, branch `docs/p00-contract-precheck`, on top of the reviewed P03a). Second half of plan phase P03; with it ABC-01 is complete. Migration `0005_runtime_ledger`: `command_requests` (idempotency by `request_id`, `payload_hash`, `pending|succeeded|failed`, ≥ 7-day retention with pending rows never purged), `runtime_events` (AUTOINCREMENT `event_id` that is monotonic and never reused across restarts, `epoch`, entity, inner `event_version`, channel, payload), and a random `workspace_id` in `app_settings`. `db/runtime_ledger.rs` writes only through `ownership::write_transaction`, so a stale owner can neither reserve, complete, nor append. `runtime/commands.rs`: `CommandEnvelope` (`deny_unknown_fields`), the nine contract error codes as a structured `CommandError { code, message, retryable }`, the whitelist (`discovery.start|pause|resume|cancel|progress|active`, `events.read`, `ownership.read`), and `Dispatcher::dispatch`: exact `research-command-v1` match → `WorkspaceMismatch` check → whitelist → for mutating commands reserve-then-execute-then-complete, replaying the FIRST outcome (result or error, verbatim) on a repeated id, `DuplicateRequest` for a reused id with other content, and `Busy`/retryable "still pending — outcome unknown, use a new requestId" when the first attempt never completed; reads are not ledgered. `LedgerSink` appends every runner event (after the runner's own commit) then forwards it; `events.read` pages `research-event-v1` envelopes by `afterEventId` and returns `lastEventId`, the reconnect being snapshot (`discovery.active`/`progress`) then cursor. Desktop: `dispatch_research_command` (rejects with the structured error) and `get_workspace_info`; the existing discovery commands now emit through the ledger sink too, so one cursor covers both entry points. TypeScript: `runtime.dispatch`/`runtime.info` in the typed client, `services/researchCommand.ts` (protocol constant, whitelist, `newRequestId`, `buildCommandEnvelope`, `isCommandError`, `shouldRetrySameRequest`) pinned against the Rust source by a `?raw` scan; the UI is not moved onto envelopes yet (P04 bridge). Tests: Rust +14 (ledger unit tests; dispatcher unit tests incl. stale dispatcher; a real `discovery.start` sent three times under one request id creates exactly one run, replays the same `runId`, refuses a changed payload, and the ledger paged by cursor equals every event the host received, in order, under epoch 1), vitest +8; reservation and ledger-append mutation-checked (each removal fails its tests). Not included: the UI on envelopes, connect mode, loopback API (P04). Validation: `cargo test --locked` 193, `npm test` 873, typecheck/build clean, plain `cargo check` zero warnings, clippy clean for the new modules; Playwright not re-run (no UI change); native Tauri not exercised. GitHub still 401 — local commit only. Handoff: `handoffs/2026-09-17-p03b-command-envelope-ledger-v1.md`. **Acceptance review (2026-09-17, Codex) R1/R2 High + R3 Medium fixed the same day**: R1 (closed on the second review) — migration `0006_request_effects` records the request's IMMUTABLE FIRST OUTCOME in `request_outcomes` (`begun` → `accepted` with the result / `rejected` with the error message), written inside the store transaction that decides it: `start` begins with the run row, is accepted with its initial checkpoint, and is rejected with its failure (a standalone rejection record when the failure has no run transaction); `resume` begins with Paused→Running and is accepted with its checkpoint; `cancel` is accepted with the cancel; a `pause` request rides on the control state and is accepted by the drained Paused transition OR by a completion that wins the race, and rejected by a run failure or a cancel — each in that transaction, with the same words `pause` then answers, and the request id is cleared only AFTER that transaction committed, so a cancel that rolls back leaves the pause to whatever resolves it later (H2 on the third review). A retry with a pending receipt replays exactly that row (never the run's later status: an accepted start stays Ok after a worker fails the run, a rejected start stays the failure for an idle run, a completion-won pause stays Ok), repairs the receipt, and executes nothing; a `begun`-only row replays an honest "never completed its admission" failure; a duplicate envelope arriving while its first attempt is anywhere between reservation and completion waits for that attempt's per-request claim (`InFlightRequests::claim`, held for the whole reserve → judge → execute/replay → complete flow — H1 on the third review) and then replays what it recorded; no row and no attempt is the first execution; a failure that changed nothing and whose receipt could not be written is answered retryable with "could not be recorded". A coordinator that fails to spawn after acceptance now fails the accepted run (status + Done event) instead of contradicting the recorded acceptance; R2 — `runtime_state.mutation_seq` is bumped inside every domain `write_transaction` (never by ledger/receipt/heartbeat writes, which use `write_transaction_quiet`), `discovery.active`/`progress` return `{ run, stateVersion }`, `events.read` returns `stateVersion` + a durable `ledgerGap` marker (written best-effort on append failure) + `ledgerDegraded`, and the reader rule (`needsResnapshot` in TS) is "empty page with a moved stateVersion, or a gap recorded strictly after the snapshot's version → re-snapshot" (M1 on the second review: a gap at or before the snapshot's version is covered by that snapshot and must not demand re-reads forever); R3 — an error that is about to be recorded is finalised (`retryable=false`, message says to send a new requestId) so the first answer and every replay agree, `retryable=true` is reserved for pending. Three deterministic regressions (`discovery_runner/tests/commands.rs`, SQLite `TEMP TRIGGER`s deny the receipt update / the ledger insert; the slot-held Busy) each fail with its fix removed; test-only store wrappers are `#[cfg(test)]`, plain `cargo check` has zero warnings.
- [x] **P03a acceptance R1/R2: Transactional epoch fencing** (2026-09-17). Fixed stale coordinator claims and check-then-takeover cancellation; every runner store mutation now receives the epoch and checks inside `ownership::write_transaction` (`BEGIN IMMEDIATE`), including admission and error paths. Added 5 tests: both reproduced handover races, cross-connection serialization, all 9 run mutation APIs, and strategy insertion. Rust **179 passed**; ordinary `cargo check` has no warnings and clippy has only 4 pre-existing core warnings. P04-only helper warnings are explicitly scoped, and test-only lock path state is excluded from normal builds. See the final Resolution in `handoffs/2026-09-17-p03a-workspace-ownership-v1.md`. No frontend change; no native UI run. Local commit only; not pushed.

- [x] **P03a — Workspace ownership** (2026-09-17, branch `docs/p00-contract-precheck`, on top of P02). First half of plan phase P03, split like ABC-02a/02b so each half is one session and independently mergeable; the second half (P03b: idempotent requests, persistent event ledger) is the next eligible phase. Implements `ownership-lease-v1` §1 of `docs/research-runtime-contract.md` in the fixed order — (1) exclusive OS file lock on `<data dir>/ownership.lock` via `std::fs::File::try_lock` (Rust 1.89, so `rust-version` moves from 1.77.2 to 1.89 instead of adding a locking crate; CI builds on stable), taken BEFORE the database is opened, released only when the owning process exits (`runtime/lease.rs`); (2) `db::open_at` + migrations; (3) migration `0004_workspace_ownership`: one row with `epoch`, `holder_kind`, `holder_instance_id`, `pid`, `acquired_at`, `heartbeat_at`, `heartbeat_seq`; `ownership::acquire` bumps the epoch, `assert_epoch` fails with the new `AppError::StaleOwner` (`db/ownership.rs`); (4) `DiscoveryRunner::with_epoch` — every runner write (start, resume, cancel, pause checkpoint, complete, fail, recovery) checks the lease first, and `CandidateAssessment.epoch` is checked INSIDE the commit transaction so a worker still reporting after the lease moved on can never write a summary, trade, record, job, or lifecycle row; (5) orphan recovery as the new epoch's holder; (6) a heartbeat thread bumps `heartbeat_seq` every 5 s, stops on drop, and `lease_is_stale(seq_before, seq_now, elapsed)` judges liveness by the reader's own monotonic interval (30 s, advisory only — it never authorises a take-over). `runtime::open_workspace(path, HolderKind)` returns an `OwnershipHandle` the desktop keeps in `AppState`; a second host gets `NotOwner` before it opens, migrates, or recovers anything; `main.rs` panics with the reason (connect mode is P04). Migration guard (contract §5.2): `apply_migrations` refuses a database whose `schema_migrations` holds a version this build does not know (`AppError::SchemaTooNew`), reads included. Tests (+12): ownership row/epoch/heartbeat/staleness; lease refused-then-released and directory creation; runtime 雙啟 refusal + take-over after release with epoch bump, a runner left over from a previous epoch cannot recover or cancel, heartbeat advances and freezes on drop, newer-schema refusal leaves the row unowned and the lock free; store-level same-assessment commit refused under the old epoch (nothing written) and accepted under the current one; boundary test covers the new modules. Each of the three guards was mutation-checked (check removed → its test fails). Not in P03a: idempotent `requestId` store, persistent `eventId` ledger, connect mode, any UI. Validation: `cargo check` / `clippy` clean, `cargo test --locked` 174 passed; frontend suites not re-run (no frontend change); native `cargo tauri dev` not exercised. GitHub still 401 — local commit only. Handoff: `handoffs/2026-09-17-p03a-workspace-ownership-v1.md`.
- [x] **P02 — Runtime decoupling (ABC-02a)** (2026-09-17, branch `docs/p00-contract-precheck`, on top of the P01 commits). Third phase of `docs/plans/active-plan.md`; acceptance "existing desktop behavior, golden, and runner atomicity unchanged; no headless scheduling added" met with a Rust-only change. (1) `TauriDiscoveryEventSink` moved verbatim from `discovery_runner/mod.rs` to the new `desktop/discovery_events.rs` (same event names, same `main` window target, same payloads); the runner keeps only the `DiscoveryEventSink` trait and no longer imports `tauri`. (2) `db::initialize(&AppHandle)` became `db::open_at(&Path)`: the desktop resolves `app_data_dir()/alphafactorforge.sqlite3` in `main.rs` (`db::DB_FILE_NAME`), the pragma order WAL → foreign_keys is unchanged (the CI smoke lane's WAL-sidecar assertion depends on it), and `busy_timeout = 5 s` (`db::BUSY_TIMEOUT`, `ownership-lease-v1` §1.2 step 2) is now set. (3) New `runtime::open_workspace(path) -> Workspace { db, discovery, recovery }` is the single open → migrate → build runner → recover-orphans path; `main.rs` setup calls it in the same position as before (single-instance plugin → setup → recovery, still asserted by `single_instance::tests`, now anchored on the runtime call) and logs a non-default recovery report to stderr. (4) `runtime::boundary_tests` reads the host-agnostic sources (runtime, db/*, discovery_runner/*, identity, error) with line/doc comments stripped and fails if any names `tauri`; mutation-checked by smuggling a `use tauri::…` into the runner. Runtime tests cover first-open directory creation + migrations + pragmas, idempotent reopen, and orphan repair on open. Runner tests (`tests.rs`, `event_contract_tests.rs`) and `execution.rs` are byte-for-byte untouched. No TypeScript, schema, migration, dependency, or command-contract change; no service binary and no scheduler (P04/P17). Validation: `cargo check`, `cargo clippy` (no new warnings), `cargo test --locked` 162 passed (+6); frontend suites not re-run (no frontend change); native `cargo tauri dev` not exercised (the CI smoke lane covers startup, and the ordering/runtime tests cover what it asserts). GitHub still 401 — local commit only. Handoff: `handoffs/2026-09-17-p02-runtime-decoupling-v1.md`.
- [x] P01 acceptance follow-up (2026-09-17) — blocked detail actions during list refresh in both records and summaries, with a synchronous handler guard that also prevents overlapping list loads. Added two reverse-order E2E regressions using the bounded `explorerRefreshDelay` mock control; both failed against `6a3bdfd` before the fix and passed afterward. `npm test` 865, build/typecheck, and all 69 Playwright tests pass. Backend unchanged; the prior 156 Rust tests / cargo check remain the backend baseline. Native Tauri restart remains untested. Both acceptance findings are resolved at the reviewed browser/mock and repository layers; see the final Resolution in `handoffs/2026-09-17-p01-acceptance-review-v1.md`. Included in the local follow-up commit; not pushed.

- [x] **P01 — Results Explorer** (2026-09-16, branch `docs/p00-contract-precheck`, stacked on the P00 commit). Second phase of `docs/plans/active-plan.md`; acceptance "re-open saved results; missing detail stated honestly; no calculation change" met. Backend (read-only, no migration): `repositories::get_backtest_result_detail` + `get_backtest_result_detail(summary_id)` command (summary + trades from one transaction, trades oldest entry first, `null` for an unknown id) and `discovery_run_id` exposed on `ValidationRecordRow` via the typed read path (writer unchanged — the run id stays a separate argument). Frontend: `services/resultsExplorer.ts` (Test-segment hiding, Validation ranking, filters, latest-summary join, persisted-null formatting, defensive `validation-record-v2` snapshot parser reporting legacy / unreadable / missing sections) and `components/ResultsExplorer.tsx` (collapsible section under the runner panel; loads on open/refresh only so a finishing background run never replaces what is on screen; selection survives refresh and a filter that hides it is reported as stale; records view with rank/gate/score/work id, snapshot detail, latest Train/Validation summaries, on-demand trades; summaries view; Test range never rendered). Mock seam: `getBacktestResultDetail`, `created_at` on saved summaries, and `?mock=1&seedHistory=1` which writes real composer-chain bundles (candle seed 34: MA 9/21 fails the default gate, MA 3/8 passes a lenient gate whose thresholds are in the snapshot) plus a manual `full` row and a hidden `test` row. **Acceptance review (2026-09-17, `handoffs/2026-09-17-p01-acceptance-review-v1.md`) fixed the same day**: R1 — trades are read as a one-transaction (summary, trades) pair and kept only when the returned summary equals the displayed row column for column (`sameSummaryRow`; id and `created_at` survive a re-save so the whole row is the identity), a mismatch is disclosed with both rows' net return / trade count and the newer trades are not attached, and a response landing after a refresh is dropped by a read generation; R2 — an `idle → loading → ready | failed` status drives the one automatic read per open, a failure keeps its message (and any earlier data) until the user refreshes, with no retry loop. Each fix has a Playwright regression driven by a mock control (`replaceBeforeDetail`, `detailDelay`, `explorerFailOnce`), and each regression was mutation-checked against the un-fixed code. Validation after the fixes: typecheck, build, 865 vitest (+11 in `resultsExplorer.test.ts`, parser exercised on composer-built records), 156 Rust (+1 detail read-back incl. same-count replacement, plus run-id read assertions), 67 Playwright (+5 in `results-explorer.spec.ts`), all green locally; native `cargo tauri dev` not exercised (command wiring compiled via `generate_handler!`, DB path via Rust tests). GitHub still 401 — committed locally only. Handoff: `handoffs/2026-09-16-p01-results-explorer-v1.md`.
- [x] **P00 — Contract & compatibility precheck** (2026-09-16, branch `docs/p00-contract-precheck`, stacked on `docs/alphabtc-capability-transfer`). First phase of `docs/plans/active-plan.md`. Added three frozen contracts — `docs/research-runtime-contract.md` (`research-command-v1` / `research-event-v1` / `ownership-lease-v1`: hosts, OS-lock-then-epoch ownership, 5 s heartbeat / 30 s stale, 5 s busy timeout, idempotent command envelope, committed event ledger, loopback control surface, storage compatibility), `docs/ai-provider-contract.md` (`ai-invocation-v1`: Codex app-server stdio adapter, restricted generation environment, `Ready`/`WaitingQuota`/`AuthRequired`/`UnknownOutcome`/`Blocked` states, budgets, information boundary, executable-DSL whitelist intersection), and `docs/market-contract.md` (`market-instrument-v1` / `market-provenance-v1` / `market-snapshot-v1`, source matrix, ETF semantics, backfill flow) — plus the living `docs/autonomous-research-capability-registry.md` listing every capability as available / plannable / blocked / unverified with the blocking reason. Local precheck evidence: `codex-cli 0.140.0`, ChatGPT login (plus plan), a quota-free app-server stdio handshake (`initialize`, `account/read`, `account/rateLimits/read`, `model/list`, `config/read`) and the generated protocol schema (261 files, kept out of the repo); Rust 1.96 / Node 24.14.1 / Tauri CLI 2.11.3; crates.io and the Binance / Tiingo / FinMind / TWSE hosts reachable. **Blocked and recorded**: AI unattended mode (generation-environment isolation unverified — 2 global MCP servers, per-thread override untested; config default model `gpt-6-astra` absent from `model/list`, which returns only `gpt-5.5`; Codex sub-commands experimental), Tiingo (no account), ETF calendar/annualization semantics, JSON DSL execution (no runtime consumer; 11 of 24 whitelisted indicators implemented in both cores). Nothing was started or imported: no research run, no market data, no code, migration, or dependency change; existing suites not re-run (docs-only phase per plan §6). `git fetch`/`gh` returned 401 (stale `GITHUB_TOKEN`), so push and PR are pending. Handoff: `handoffs/2026-09-16-p00-contract-precheck-v1.md`.
- [x] **ABC-DOC-001** — AlphaBTC capability inventory and transfer specifications (2026-09-15). Recorded 20 source-grounded capabilities, 15 bounded follow-up specifications (ABC-01, 02a, 02b, 03–14), and the corrected product requirement that the desktop UI and independent continuous service can coexist. Added only the handoff and this task-board entry on `docs/alphabtc-capability-transfer`, based on locally known `origin/main` `e3fc79f`; original feature branch/commits retained. Validation: local file/link/ID consistency and diff checks; no engine/test/DB changes, no runtime suites or service/AI execution. Pre-PR review verified every C01–C20 source claim and numeric figure against both repositories and moved recovery authority to the workspace-lease holder so ABC-01/03 stay compatible with RUNNER-OWNERSHIP-001 and the PR #103 smoke lane. Feature implementation remains Backlog.

- [x] **IO-ROBUSTNESS-001 (P2)** — report writes can no longer clobber each other (2026-08-18, branch `fix/atomic-report-filenames`, branched from `origin/main` `e3fc79f`).
  - `unique_report_path` chose a name with `Path::exists()` and `save_report` then called `std::fs::write`, which **truncates**: anything created between the check and the write was silently destroyed, and the loser was never told — `save_report` returned the path it believed it had written. The old timestamped last resort had the same shape.
  - Naming and creation are now one atomic step via `OpenOptions::create_new`: the OS refuses the open if the path appeared meanwhile, so a collision becomes a retry under the next candidate name instead of data loss. **Behaviour change:** exhausting the candidates now fails with `no unused name for "<file>" after 1000 attempts` instead of writing a timestamped file — failing loudly is the point, since the old fallback could still truncate.
  - **Mutation evidence.** Reverting only `.create_new(true)` to `.create(true).truncate(true)` — exactly `fs::write` semantics — makes all four new tests fail, and the barrier-synchronised concurrency test fails with the real signature `left: "writer-1", right: "writer-0"`: writer-0's report holding writer-1's bytes. The regression reproduces the data loss, not just a naming difference.
  - Tests: plain-name-then-suffix ordering with per-file contents, an existing report never truncated, eight barrier-released concurrent writers each getting their own file with its own bytes and no file merged or lost, and exhaustion failing loudly with every existing file intact. `safe_report_filename`'s test is kept verbatim — that contract is unrelated.
  - No frontend change (`files.saveReport` keeps its signature) and no schema change. Validation: `cargo test --locked` (**158**, +3), `cargo check --locked` (no warnings), typecheck, `npm test` (854), build, `npm run e2e` (62).
  - **Review follow-up (PR #105).** `write_new_report` had been given `pub(crate)` visibility, which quietly moved a trust boundary: it takes an arbitrary `file_name` and hands it straight to `dir.join`, and `join` **replaces** the base when the argument is absolute — so any backend module could have escaped the downloads directory and the `.json`/`.csv` contract that `safe_report_filename` enforces for `save_report`. It is private again, with the reason recorded on the function; the nested test module reaches it as a private parent item, so nothing was lost. This is the second consecutive review to catch the same shape (see PERSIST-INVARIANT-001's `insert_backtest_summary`), so the rule is now explicit: extract at the narrowest visibility, and if a helper must be crate-visible, move the validation into it rather than trusting callers.

- [x] **PERSIST-INVARIANT-001 (P2)** — reject a summary that contradicts the trade rows beside it (2026-08-16, branch `fix/persist-bundle-invariants`, branched from `origin/main` `2b80be9`).
  - `save_backtest_result` accepted **any** bundle: a `trade_count` that disagreed with the rows, a side outside `LONG|SHORT`, an exit before its entry, trades outside the summary's own range, a non-positive price, or a non-finite metric. The schema cannot catch any of it — its CHECKs cover single columns, not agreement *between* a summary and its rows — and a stored contradiction is worse than a rejected write, because every later reader trusts the row.
  - `validate_result_bundle` is enforced at `write_backtest_result`, the funnel all three writers pass through (manual save, manual validation bundle, runner candidate commit), so a future fourth writer cannot forget it. Nothing is written before the check, and every caller wraps it in a transaction, so a rejection leaves the previous result exactly as it was.
  - **Strictness is deliberately limited**: only definitionally bounded ratios are bounded (`win_rate`, `exposure` in `[0, 1]`), while `max_drawdown` and `turnover` must be non-negative but are **not** capped at 1 — a short position can lose more than the starting equity, so a real drawdown may exceed 100%. Everything else is required only to be finite, which is the existing contract: the metrics mapper narrows a legitimately infinite profit factor to NULL, so a non-finite value in a column means the mapper was bypassed.
  - **19 existing tests failed on the first run, and every one was a fixture describing impossible data rather than a product defect** — details in the backlog spec. The store tests committed token trade slices beside summaries from a real assessment whose `trade_count` is 179; restating that count to match the tokens was tried and rejected, because PERSIST-AUDIT-001 already requires it to equal the immutable record's metric snapshot. The two validators together leave exactly one self-consistent choice — commit the assessment's own trades — which is what the real runner does anyway. One rollback test also injected its failure with an illegal segment, which this gate now catches *before* any insert, silently turning a rollback proof into a rejection proof; it now uses a dangling `dataset_id`, which no pure validator can see and which fails as a foreign-key violation after the first summary has already written.
  - Mutation coverage: 20 cases, each starting from the same valid bundle so a rejection can only come from the one field it changes, plus a test proving an impossible replacement leaves the stored summary and trades byte-for-byte unchanged with no extra or orphaned rows.
  - **Review follow-up (PR #104, request-changes → fixed).** `insert_backtest_summary` was still `pub`, so the raw writer — which takes no trade rows and therefore cannot check anything — remained reachable from any module. That left "no writer can forget the invariants" as a convention rather than something the compiler enforces, so it is now module-private with its only caller being the validating funnel; Rust visibility is the defence, no runtime test needed. The review also caught a factual error in a comment: `RESULT_SEGMENTS` does mirror a real CHECK in migration 0001, but `trades.side` has **no** CHECK there (only a `-- LONG | SHORT` comment), so this validator is that vocabulary's only enforcement — the comment claimed a database guarantee that does not exist.
  - No schema, migration, or frontend change. Validation: `cargo test --locked` (**155**, +3), `cargo check --locked` (no warnings), typecheck, `npm test` (854, unchanged), build, `npm run e2e` (62, unchanged).

- [x] **CI-TAURI-SMOKE-001a** — the first CI lane that builds and RUNS the real desktop binary (2026-08-16, branch `feat/native-tauri-smoke-lane`, branched from `origin/main` `1c5d4f0`).
  - Until now **no lane had ever started the app**: `cargo check`/`cargo test` never link the binary, and the Playwright suite drives the React tree against the in-memory `?mock=1` client by design. Everything in `main.rs`'s `setup` — app-data resolution, database creation, migration application, and orphan recovery, all behind `.expect()` — was verified only by hand.
  - New `native-smoke` job on `windows-latest`: `tauri build --debug --no-bundle` (a debug build links the same binary and runs the same startup path as release in a fraction of the time; `--no-bundle` skips installers this lane never needs), then a launch smoke that fails if the app exits early, if the SQLite database or its WAL sidecar is missing or empty (the sidecar is the half that actually proves migrations ran — WAL mode is set before they are applied, so the main file stays at one 4096-byte header page either way), or if **no `msedgewebview2` process appears** — the last one is what separates "the process is running" from "the window actually rendered". The lane also refuses to pass on a pre-existing database, so a leftover file from an earlier attempt cannot make it vacuous.
  - **The scripted invoke/event round trip is deliberately NOT here.** It needs a WebDriver stack (`tauri-driver` + `msedgedriver`), which the no-new-dependencies rule reserves for maintainer approval, so it stays `CI-TAURI-SMOKE-001b`. This slice proves the binary links, starts, renders, and completes its startup persistence path — not that a command round-trips.
  - Also carries the PR #102 review's non-blocking documentation item: `DiscoveryPanel.tsx`'s header still described the single-counter sequence guard that the review rejected. It now states the per-state-slice rule, why a single counter is wrong when one channel is throttled, and that the rejected design must not be reintroduced.
  - No dependency, lockfile, or product-code change. Validation: typecheck and `npm test` (854) locally; the native build verified locally; the launch assertions are exercised by CI on the PR itself.

- [x] **RUNNER-UI-001b-2** — the discovery runner panel (2026-08-16, branch `feat/discovery-runner-panel`, branched from `origin/main` `bb0d38b`). **The backend runner merged in RUNNER-EXEC-001 is now reachable from the product for the first time.**
  - Added `src/components/DiscoveryPanel.tsx`, mounted in `BacktestPanel` beside the sweep and reading the same `liveContext`. It starts a run over one parameter axis, previews the candidate count from the shared `axisValues`, and offers pause / resume / cancel / re-query. The seed is generated by `randomRootSeed()` but **displayed and editable** (the Random Entry distribution derives from it, so reproducing a run means re-entering it), and the holding allowance is an explicit field, per VAL-003.
  - **Two invariants carry the design.** (1) The database is the source of truth: `getActiveRun()` on mount adopts whatever run exists — the path by which a run that startup recovery left `paused` is rediscovered after a window reload — and events are only a fast path on top of that snapshot. (2) All event ordering lives in one pure reducer (`services/discoveryFeed.ts`) with a forward-only applied sequence **per state slice** — one for status/counts, one for the result list — so a replayed or out-of-order event is ignored and a coalesced progress tick cannot overwrite a terminal status. A single counter shared by all three channels was tried and rejected in the PR #102 review: progress is throttled and results are not, so a coalesced tick arriving after a newer result would look stale and lose its counts.
  - **A real bug the e2e caught**: cancelling the throttle on a terminal event also drops whatever progress tick was still queued, so a fast run ended showing `完成 1/3 · 已完成`. The terminal path now re-reads the authoritative counts from the database rather than reconstructing them from whichever events survived coalescing — invariant (1) doing actual work, not a formality.
  - Panel-owned contracts: none. The envelope comes from b-1 (validated before any command is called, so an invalid axis/seed/strategy creates no run row), events arrive parsed and version-checked from slice a, and a rejected payload becomes a zh-TW "may be behind the database, re-query" notice instead of being swallowed. Gate-failed candidates show no score rather than a zero.
  - The `?mock=1` seam gained a fake runner that emits **real `discovery-event-v1` wire payloads** (camelCase, omitted optionals, explicit null score) through the production subscription functions and parses them with the production parsers, so the browser specs exercise the whole chain instead of a convenient shape. Two DEV-only knobs: `discoveryStep=<ms>` paces simulated candidates and `discoveryRun=paused` pre-creates a paused run, without which recovered-run adoption has no observable path in a single e2e.
  - Scope: no Rust, no migration, no contract change, and no Results Explorer (still its own Phase B task). One axis only — multi-axis is a product decision, not a contract limit.
  - **Review follow-up (PR #102, request-changes → fixed).** Independent acceptance found two blocking races and one error-handling gap, all of which the mock's original timer pacing hid:
    - **The sequence guard was not monotonic.** It lived in refs re-derived from React state each render, but a result event advances the applied sequence without advancing `run.lastSequence` — so the next render wrote the ref *back* to the older value and a replayed event was accepted twice. Ordering that depends on render timing is not ordering, so it moved into the pure `src/services/discoveryFeed.ts` reducer (17 named tests). The guard is now per state slice — `statusSequence` for status/counts, `resultSequence` for the list — because progress is throttled and results are not, and a single counter would make a coalesced progress tick that lands after a newer result look stale and drop its counts. Delayed snapshot reads are also rejected when older than what events already established.
    - **Events emitted before `start_discovery` returned its run id were lost.** The backend emits its first progress event and spawns the coordinator *before* returning, so a short run can emit results — even finish — while the WebView still awaits the command. Counts survive a snapshot re-read but result rows do not: no command returns result history. The reducer now buffers events while no run is followed and drains them on adoption. A new `discoveryEmitBeforeStart=1` mock knob reproduces the production ordering, and a mutation check confirms every result row disappears without the buffer.
    - **A rejected `listen` leaked the channels that had already registered** and surfaced only as an unhandled rejection. Subscriptions now register one at a time inside a try/catch that cleans up partial successes and reports the failure in the UI; `discoverySubscribeFail=<channel>` makes that path deterministic, and the e2e also asserts no page error is raised.
  - Validation: `npm run typecheck`, `npm test` (**854**, +17), `npm run build` (bundle 271.23 → **296.09 kB**, gzip 88.83 → 96.40 kB: the `discoveryConfig`/Gate/Score/RandomEntry graph enters the UI for the first time, as the spec predicted), `npm run e2e` (**62**, +6, every existing spec unmodified and green with the panel mounted).

- [x] **RUNNER-UI-001b-1** — assemble and pre-validate the `discovery-config-v1` envelope in the frontend (2026-08-16, branch `feat/discovery-run-config`, PR #101, merge `bb0d38b`).
  - Added the pure `src/services/discoveryRunConfig.ts`. Starting a run means submitting the full admission envelope — thirteen exact keys, ten pinned contract versions, dataset identity, base preset with axes, complete Gate/Score configs, explicit seed, caps — so hand-writing it in a component would have created a second, drifting copy of a contract the backend rejects wholesale.
  - **One admission authority**: the builder validates with `parseDiscoveryConfig`, the same parser the Rust side mirrors, so a malformed run fails in the workspace with a path-qualified message and **no run row is ever created**. The builder adds no rule of its own. The STRATEGY-VALIDATION-001 period rules therefore reach discovery for free, via the parser's `checkNumericParam` pass over the base preset.
  - **Derive, do not ask**: `contracts`/`gateConfig`/`scoreConfig` are copies of the owning constants, and `benchmarkCosts` is derived from the base strategy because the envelope requires the two to agree. `maxConcurrency` is always `null` in v1 — the backend resolves it with ITS core count, so a number validated against the WebView's `hardwareConcurrency` could be admitted here and rejected there. The strategy is deep-cloned, so a later keystroke cannot change what a submitted run recorded.
  - **Three findings pinned as behaviour rather than assumed away** (details in the backlog spec): an empty axis list is a legal single-candidate run and the builder must not invent a "needs an axis" rule; the candidate cap is enforced by `enumerateCandidates`, not by envelope admission, so an over-budget grid builds here and is rejected by the backend before any job exists (the per-axis value limit, by contrast, does fail locally); and `randomRootSeed` needed a clamp, because `floor(1 * (MAX_U32 + 1))` is one past the admissible range for an injectable generator that can return 1 — a defect my own boundary test caught.
  - Scope: no UI, no `mockClient`, no e2e, no Rust, no command invoked. The panel is `RUNNER-UI-001b-2`.
  - Validation: `npm run typecheck`, `npm test` (837, +20), `npm run build` (bundle unchanged at 271.23 kB, confirming the builder is not yet in the UI graph), `npm run e2e` (56, unchanged). No Rust file changed, so `cargo` was not re-run.

- [x] **RUNNER-UI-001a** — the typed, version-checked frontend boundary to the merged discovery runner (2026-08-16, branch `feat/runner-event-contract`, PR #100, merge `c236b1b`).
  - `src/tauri-client/events.ts` was written before the runner existed and matched **none** of the emitted fields (it expected `tested`/`total`/`skipped` and `current: { symbol, interval, segment }`). Nothing imported it, so nothing failed — the first UI built on it would simply have rendered `undefined` everywhere. It now mirrors `DiscoveryProgressEvent` / `DiscoveryResultEvent` / `DiscoveryDoneEvent` field for field.
  - **The contract is pinned from both languages against one authored fixture**, `fixtures/rs-core/discovery-event-v1.json`. Rust asserts `serde_json::to_value(struct) == sample` (new `discovery_runner/event_contract_tests.rs`, 7 tests); TypeScript asserts its parsers accept the same samples. Unlike every other `fixtures/rs-core` file this one is **hand-authored on purpose**: Rust emits and TypeScript consumes, so neither side may generate the other's expectations. The fixture covers both opposite optional conventions the payloads really use — `candidate`/`bestStrategyId`/`errorMessage` are **omitted keys** (`skip_serializing_if`) while a gate-failed `score` is an **explicit null** — which is precisely the asymmetry a hand-written DTO gets wrong.
  - Payloads are parsed, not trusted: a wrong/missing `eventVersion`, a missing required key, a wrong runtime type, an unknown run status, an integer outside the JavaScript safe range, a non-finite score, and a present-but-malformed optional are each rejected by a named test. A rejected payload is dropped and reported through an `onInvalid` hook rather than reaching the UI as `undefined`; unknown *extra* keys are tolerated so a backend addition cannot break a live window, since real drift is caught at build time and any observable payload change must bump the version.
  - Added the `get_active_discovery_run` wrapper the frontend never had — the only way to rediscover a run that startup recovery turned into `paused` — and typed `get_discovery_progress` as `DiscoveryProgressSnapshot` instead of `unknown`.
  - `throttle` became `createThrottle(fn, ms, { now })` returning `{ call, cancel }`. The old one could not be cancelled (a queued trailing call fired into an unmounted view) and could deliver the same payload **twice** when the window reopened before its queued timer ran. A mutation check reproduces the old behaviour as `[1, 2, 2]` and the new test catches it; the clock is injectable so the window is not tested through timer internals.
  - Scope: no UI, no `mockClient`, no e2e, no migration, and **no change to any emitted payload, event name, command name, or command signature** — this slice makes the frontend match the backend, never the reverse. The runner panel is `RUNNER-UI-001b`.
  - **Review follow-up (PR #100, request-changes → fixed).** Independent acceptance found one blocking contract defect: `score` has no `skip_serializing_if`, so its key is ALWAYS emitted and only the value is nullable — but the parser treated an absent key as "no score", which would have silently accepted a payload that lost the field and masked exactly the drift this slice exists to catch. The key is now required, the value must be `null` or finite, and `gatePassed === (score != null)` is enforced (Score is only computed for a Gate-admitted candidate, and `validation_records` enforces the same pairing with a CHECK). Added the named missing-`score` test, both contradiction directions, `score`/`sequence`/`runId` in the missing-key matrix, and the listener-level tests the acceptance criteria implied but nothing covered — proving all three channels drop rather than forward an unparseable payload and report it through `onInvalid`. The contract comment that invited the defect ("accepts either spelling") was rewritten to separate omitted-optional from always-present-nullable fields.
  - Validation: `npm run typecheck`, `npm test` (817, +44), `npm run build`, `npm run e2e` (56, unchanged), `cargo check --locked`, `cargo test --locked` (152, +7).

- [x] **STRATEGY-VALIDATION-001 (P1)** — one runtime admission gate for a manual strategy's indicator parameters (2026-08-16, branch `fix/strategy-period-validation`, PR #99, merge `cbc3f42`).
  - Added the pure `src/services/strategyValidation.ts`. Eleven indicator-grid fields are hard-validated — eight periods as safe integers `>= 1`, `rsiBuy`/`rsiSell` inside 0–100, `bbMult` positive — at two fail-closed mount points: `runParamsBacktest` (every manual execution, including each sweep variant) and `buildStrategyDef` (before a durable `strategy-v2` identity exists). `fastMA: 2.5` previously reached `sma()`, where `values[i - 2.5]` is `undefined`, so every value became `NaN` and the run reported a confident **zero-trade success** that could be saved and exported.
  - **The rule was not duplicated.** `checkNumericParam` remains the sole authority and an agreement test compares the two over a value battery for all eleven keys. The five execution-model fields are deliberately excluded and documented: `toExecCostFractions`' legacy clamping (`sizePct: 0` = 100%, negative fee → 0) is a committed contract with its own tests, and the engine's `assertNormalizedFraction` already owns the upper bound.
  - **Cross-field rules are warnings, not errors** — `fastMA<slowMA`, `macdFast<macdSlow`, `rsiBuy<rsiSell` are computable-but-dubious hypotheses, the repo's own recorded judgment prunes rather than rejects them, and blocking them would blank the (20,20) cell of the default 2-D sweep. **Adjudicated and closed** (maintainer, 2026-08-16): warnings stay, because these rules constrain hypothesis quality rather than computability, a manual strategy may deliberately invert its parameters, and the affected indicator is not necessarily read by the selected signal — discovery may prune them in its search space without forbidding manual execution or saving. Do not re-open by making them fatal.
  - **Forced dependency inversion — accepted by the maintainer (2026-08-16).** `discoveryConfig -> randomEntry -> backtestRunner` means a validator the runner imports cannot import `discoveryConfig`; the first attempt produced an ESM cycle that broke 26 existing tests with `discoveryConfig.contracts is missing key "gate"`. The domain table, `checkNumericParam`, `DISCOVERY_VALIDITY_RULE_IDS`, and `candidateValidity` therefore moved **verbatim** into the new module, and both discovery modules re-export them. Neutrality is mechanically proven: `discoveryConfig.test.ts`, `candidateEnumeration.test.ts`, and `runnerConfigFixture.test.ts` pass **unmodified**, and regenerating `benchmark-v1.json` / `runner-config-v1.json` changed **only** three `generator.sourceHashes` lines — every expected value byte-identical.
  - UI: the Run button disables with a zh-TW list naming each offending field (mirroring the existing invalid-code-expression pattern), the offending inputs are outlined, warnings render separately, and the eleven inputs gained `min`/`step` hints as *secondary* protection — `min` repairs `0` on blur but cannot repair `2.5`, which is exactly why the runtime gate exists. A strategy saved before this contract still loads into the editor so it can be repaired (`strategyFromDef` unchanged; there is no delete in the UI yet — PARITY-003).
  - Validation: `npm run typecheck`, `npm test` (773, +30), `npm run build` (bundle 268.37 → 271.19 kB raw, 87.90 → 88.82 kB gzip), and `npm run e2e` (56, +3) pass. No Rust file changed, so `cargo` was not re-run; the two regenerated fixtures are data-only and structurally identical.

- [x] **BUG-SWEEP-CONTEXT-001 (P1)** — bound every completed parameter sweep to the inputs it optimised over (2026-08-15, branch `fix/sweep-result-context`, PR #98, merge `be4e3c6`).
  - Added the pure `src/services/sweepArtifact.ts`: a frozen `CompletedSweep` whose `sweep-context-v1` context carries the dataset id/hash/symbol/interval/time range/bar count, the exact optimised bar range including the Holdout split, the normalized sweep configuration, and the base strategy **with the swept axes removed**. One comparison point (`sweepContextKey` / `sameSweepContext` over `core/hashing`'s `canonicalize`) decides whether a grid still describes the live inputs; `SweepSection` never compares dataset, strategy, or Holdout fields itself, so no call site can forget one and a future `ParamsStrategy` field participates automatically.
  - The audit's reproduction is closed: a grid optimised with Holdout OFF no longer survives switching Holdout ON, so 套用最佳 can no longer apply a combination that was chosen using the out-of-sample tail. Moving the Holdout percentage, switching dataset, and editing any field the sweep held constant invalidate the same way. The gate is derived during render, so the heatmap, 套用最佳, and the ✓ applied marker disappear in the same render that accepts the edit, replaced by a zh-TW `sweep-stale` notice.
  - **Masking is deliberate**: applying a swept-axis cell writes exactly the parameters the sweep varied, so it must keep the grid valid — that intentional case is asserted in both the unit tests and the E2E. Every non-swept field stays in the compared basis; a `describeSweepBasis` test derives the expected key set from `defaultStrategy()` rather than listing it by hand.
  - The optimised range is derived from the panel's run range by one helper (`sweepRangeFromRunRange`), so BUG-001's in-sample boundary stays single-sourced with `run()` and with the sweep's own `from`/`to`. `SweepSection` now takes one `liveContext` prop instead of `strat`/`interval`/`datasetSelected`/`holdout`/`holdoutPct`.
  - A generation token plus an owner ref discards a late completion whose inputs are no longer live, leaving neither a grid nor an error; the predicate `sweepResultIsWritable` is pure and unit-tested on all four branches, because the repo has no React component-test harness and forcing that race through Playwright would be timing-dependent.
  - Scope: no change to `paramSweep.ts`, `runArtifact.ts`, `src-tauri/`, schema, migrations, dependencies, or any existing `e2e/` spec; the sweep artifact is in-session provenance and is never persisted. `freezeDeep`/`cloneDeep` are duplicated from `runArtifact.ts` rather than shared, to keep the merged BUG-RESULT-CONTEXT-001 contract file out of this diff — extracting one `services/immutable.ts` is recorded as a follow-up suggestion.
  - Validation: `npm run typecheck`, `npm test` (743, +40), `npm run build`, and `npm run e2e` (53, +3) pass. No Rust file changed, so `cargo` was not re-run. Mutation check: with the context gate forced true, the two gate E2E specs fail on exactly the intended assertions and the dataset-switch spec still passes — that one is protected by the panel unmounting the section when candles clear, and the dataset fields' contribution to the gate is proven by the unit tests instead.

- [x] **MARKETING-CAMPAIGN-001** — completed a repository-grounded Traditional Chinese product marketing campaign for AlphaFactorForge (2026-08-09, branch `feat/product-marketing-campaign`).
  - Added the stable `showcase/` package: five purposeful 2400×1350 campaign images, the declared `hero.png` alias, a dedicated 1200×900 thumbnail, a five-image contact sheet, manifest/campaign indexes, and all six canonical supporting metadata files.
  - Selected the distinct **鍛件檢驗證書** direction: warm mill-test-report paper, graphite registration rules, safety-orange actions, teal validation marks, authentic dark product UI evidence plates, and deterministic Traditional Chinese typography. The generated background contains no UI or copy; all product proof comes from isolated SAMPLE demo captures whose pictured sources are unchanged through the campaign commit.
  - Claims are traceable to implementation and E2E evidence for the complete backtest workspace, Holdout comparison, 1–2 axis / 256-combination in-sample sweep, bar replay, immutable result context, and JSON/CSV export. AI strategy generation is recorded as the qualified core product goal; only the stronger claim that its complete frontend/provider flow already ships is rejected, alongside overfitting-elimination and demo-performance claims.
  - Validation: strict campaign contract 0 errors / 0 warnings; 8/8 PNGs decode, pass dimensions/blankness/privacy/product review, and every output pixel is fully opaque. The only image-QA warning is the manifest-declared identical `hero.png` / `01-hero.png` alias.
  - Integrated all five useful campaign images into the root `README.md`, leading with the AI-strategy-generation → historical-validation product loop and clearly separating that goal from currently shipped UI capabilities.

- [x] **DATA-QUALITY-001 (P1)** — added the `market-data-quality-v1` admission contract so dataset identity is no longer mistaken for dataset validity (2026-08-09, PR #94, merge `bd445f3`).
  - One ordered rule set (plausible epoch-millisecond timestamps in `[2000-01-01Z, 2100-01-01Z)`, independently asserted representability, positive OHLC prices, non-negative volume, `high >= low`, `low <= open/close <= high`) is implemented once per language and enforced at four mount points: `dbClient.prepareDatasetImport`, the `BacktestPanel` candle load, `repositories.rs` before its transaction opens, and the runner's `load_verified_dataset` after identity verification. Datasets are admitted whole or rejected whole.
  - Data stored before the contract fails closed: a rejected `start` writes nothing at all (no run row, job, progress, event, or coordinator control), a paused run stays paused on rejected `resume`, and the UI keeps Run/Save/Export disabled through the existing `liveContext == null` guard with zh-TW re-import guidance. The stored-invalid regression inserts its dataset via raw SQL with a hash computed over the invalid candles, and asserts the rule id **and** that the failure is not an identity mismatch.
  - The dataset hash preimage, its version string, and `identity-v2.fixture.json` are unchanged; `identity.rs`'s timestamp `1`/`2` hashing tests still pass unmodified. No schema, migration, dependency, or `e2e/` change.
  - Known limitation recorded in `docs/market-data-quality-contract.md` §5: rule 1 is structurally unreachable at the import mount point, because the pre-existing identity safe-integer check fires first and `db::Candle.timestamp` is an `i64`. Its mutation case asserts the observed rejection plus the full atomicity guarantee. **Adjudicated and closed** — the maintainer accepted the implementation as written and ruled against reordering identity and quality validation; the reasoning is in the Implementation Resolution of `handoffs/2026-08-09-data-quality-001-planning-decisions-v1.md`. Rule-id classification is owned by the validator's own API and the parity fixture, which carries four rule-1 rejection rows.
  - Validation: 703 Vitest + 145 Rust + 50 Playwright E2E, typecheck, production build, `cargo check`/`test` `--locked`, Clippy with only the four pre-existing `backtest.rs`/`score.rs` warnings, and a double fixture regeneration with matching SHA-256 digests. `INTERVAL-CONTRACT-001` remains an open, separate decision; `BUG-SWEEP-CONTEXT-001` is next.

- [x] **RUNNER-OWNERSHIP-001 (P1)** — registered `tauri-plugin-single-instance` 2.4.3 as the first builder plugin, before `setup`, SQLite initialization, and discovery orphan recovery (2026-08-09, PR #90, merge `a451f0d`).
  - A second desktop launch now exits cleanly after the primary process shows, unminimizes, and focuses its `main` window. Three Rust regressions lock the operation/error order and the plugin → setup → recovery source order.
  - The dependency is desktop-target-only. Its official MSRV raises the declared project floor from Rust 1.77 to 1.77.2; no schema, command, event, frontend, or capability change is required. An owner-generation/lease task is not added because v1 remains a deliberately single-process desktop architecture; add defense-in-depth only if multi-process execution becomes a product requirement.
  - Windows native smoke used an isolated Tauri identifier: the second process exited 0, exactly one binary remained, the minimized primary became visible and foreground-focused, and an injected live run remained `running` instead of being paused/requeued. Validation: 690 Vitest + 135 Rust tests, typecheck, production build, cargo check, targeted rustfmt, Clippy with only the four pre-existing warnings, and `git diff --check`.

- [x] **PERSIST-AUDIT-001 (P1)** — upgraded new immutable assessment writes to validation-record-v2 with an explicit metrics-v2 pin and strict version-dispatched Rust DTO validation (2026-08-09, PR #89, merge 1143abb). Existing v1 rows remain readable legacy evidence; new v1/unknown writes fail closed. Validation: 690 Vitest + 132 Rust + 50 Playwright E2E, typecheck, build, cargo check, and CI all green.

- [x] **SKIN-002** — completed the existing ten-skin system with three generated local WebP workspace backgrounds (paper, aurora, industrial orange), per-skin scrims, CSS scanline/grid patterns, and intentionally flat skins. Split opaque contrast colours from visual background layers; fixed persisted-id fallback, cross-window live switching, focus rings, duplicate swatch keys, responsive columns, native colour schemes, token-sized pop-out headers, reduced motion, and the floating-chart minimum height. Review follow-ups added an independent 520px accessible-name guard, made Aurora's text-bearing `surface2` a deep glass with a 0.5 contrast-ratio safety margin over AA in both nested and free-standing placements, and removed the unused `color.surface` / `--afs-surface` dead token. Added unit and Playwright regressions; visual QA covered full-chart aurora and signal-orange screens (2026-08-07, branch `feat/theme-backgrounds-quality`). Handoff: `handoffs/2026-08-07-project-review-and-skin-002-v1.md`.

- [x] **METRIC-002 (P1)** — corrected UTC monthly-return baselines and bumped the persisted calculation contract to `metrics-v2` (completed locally 2026-08-07 on `fix/monthly-return-baseline`).
  - The first present month now uses resolved `startEquity`; each later present month uses the prior present month's close, gap months stay absent, and a non-positive base yields 0 for that month while still carrying its close forward. TypeScript and Rust share the algorithm and hand-calculated cases, including the audit's `[0, +1, -0.5, +1]` series and Score consistency `0.13341888991522402`.
  - All five active declarations/pins, two literal assertions, the Rust module contract, two current contract documents, and all four generated parity fixtures moved to `metrics-v2`. Two complete regeneration passes produced identical SHA-256 digests: backtest `63841838A55835EA49A6AD31C30FEE1EC7249BC4F800FDCFBFFE4342E29BBBF3`, benchmark `37C6825509D7A683F347C8CFDA5BFC8BD7395A3479AAD9D7D69E46063DAE3B0F`, gate-score `19E47AA75D946542A26EF00929CF03578CEA4ABA571C45F7853AA1D5C58335DC`, runner-config `75EDF4715C59DB81DFF0B03E3C353E9FCF23526CE78494FCAB50AB02550D6E7B`.
  - A real paused-run DB read-back regression proves stored `metrics-v1` config is rejected without changing run, progress, jobs, events, SQLite write count, or coordinator controls. Validation: typecheck; 667 Vitest; production build; 130 Rust tests; 42 Playwright E2E.
  - `PERSIST-AUDIT-001` remains required: existing `validation-record-v1` rows have no metrics contract and stay legacy evidence with an unknown formula version.

- [x] **SKIN-001** — ten switchable interface skins driven by one token contract (2026-08-02, PRs #80 / #81 / #82 / #83 / #84;施工單 `handoff/AGENT_TASK.md`, 設計規格 `handoff/README.md`, both from the `AlphaFactorForge 皮膚系統.zip` drop). Handoff: `handoffs/2026-08-02-skins-integration-v1.md`.
  - PR-A (#80) added `src/theme/{theme,themeCss,ThemeProvider}` and wrapped every mounted tree. PR-B (#81) turned `panelStyles` into `makeStyles(theme)` and removed the colour literals from `src/components/**` and the app shell. PR-C (#82) added `src/charts/chartPaint.ts` and made the canvas and sweep heatmap read `theme.chart`. PR-C2 (#83) added the `ema`/`bb` chart tokens and a volume-colour toggle. PR-D (#84) mounted `SkinPicker` and added the skin e2e.
  - Visual equivalence was NOT achieved at PR-A and this was accepted deliberately, not overlooked: the repo named `IBM Plex Sans/Mono` in CSS but never loaded them, so the provider's font link changed the whole UI's typeface; the handoff stylesheet also introduced `body{margin:0}`, `*{box-sizing:border-box}` and a token-derived focus ring. Owner accepted all of it on 2026-08-02. Details and measurements in the handoff note and in PR #80.
  - Contrast pass (2026-08-04): the spec's `muted` AA item named two skins; an audit found three (atelier-warm was unlisted) and the same value also sat in `header.muted` / `chart.label`, so all three tokens were raised in all three skins. The four previously unmapped colours became tokens — including a new `color.accentWash` for the sweep-applied field, because a pale blue wash is wrong on the dark skins — so `grep -nE "#[0-9a-fA-F]{6}" src/components/` now returns comments only, meeting the PR-B acceptance line in full. `src/theme/contrast.test.ts` locks it. `faint` was resolved by moving misuse rather than raising the token: eight of its eleven call sites carried text the user must read (empty-state instruction, the no-rules warning, bar and combo counts) and moved to `muted`; the three genuine de-emphasis uses stayed and `faint` was raised only to 3:1, so the ink/muted/faint hierarchy survives. Remaining known gap: swiss-forge white-on-signal-red is 4.31:1 — raising it means changing the skin identity colour, so it is asserted at >=4.3 and left for the designer.
  - Verified against the design mock on 2026-08-04, after the designer supplied the `.dc.html` missing from the first drop: token values matched exactly across all ten skins; five canvas-painting gaps (last-price tag, volume opacity, dashed `ma2` in the OHLC skin, always-dashed RSI guides, axis type) were closed, and heat-cell ink moved from a ramp-position proxy to a measured contrast choice. The design mock's own `#111111`/`#f2f2f2` ink pair floors at 4.112:1, under AA, so the ink pair was changed to pure black/white (owner-approved): measured floor is now 4.587 and the test asserts >= 4.5.
  - Deferred by the施工單 itself: PR-E (Strategy Library / Discovery Runner screens) is design reference for screens that do not exist yet. When they are built, use `makeStyles(theme)` and the existing `card` / `tableRow` / `tableHead` / `chip()`; do not define new colours.

- [x] **BUG-RESULT-CONTEXT-001 (P1)** — bind every completed interactive backtest to an immutable execution snapshot (2026-07-31, `fix/backtest-result-context` @ `8a8015f`, branched from `origin/main` `0ebe6a8`; PR #78 merged 2026-08-01 as `38df26a` after independent acceptance).
  - Added the pure `src/services/runArtifact.ts`: a frozen `CompletedRun` carrying a deep strategy snapshot, the durable `strategy-v2` identity, the dataset id/hash/symbol/interval/time range/bar count, and the exact traded range including the Holdout split. Snapshots are `structuredClone`d (not JSON round-tripped, so a legitimately infinite profit factor survives) and deep-frozen, so nothing the editor keeps mutating can reach a finished run.
  - One comparison point. `runContextKey`/`sameRunContext` (over `core/hashing`'s existing `canonicalize`) decide whether a completed run still describes the live inputs; no React handler compares strategies or datasets field by field, so no call site can forget a field, and a future `ParamsStrategy` field participates automatically. `describeRunRange` derives the split from the same `holdoutSplitIndex` the run executes, so the recorded range cannot drift from the executed one. A null context is never equal to anything, so "not loaded yet" and "load failed" both fail closed.
  - `BacktestPanel` now holds that artifact instead of a bare result, and renders/saves/exports only through a derived gate (`completed` AND context still matching). The gate is computed during render, so a result-affecting edit invalidates Save/Export/pop-out in the SAME render that accepts the edit. One `changeStrategy` path covers mode, signals, rules, code, indicator periods, execution/risk fields, the chart quick row, library load, and Sweep apply; `invalidateRun` covers the Holdout toggle and percentage.
  - Candle readiness is stored as `{ datasetKey, rows }`, so candles are only visible when they are proven to belong to the selected dataset — a non-empty array is no longer sufficient. Selecting a dataset drops readiness and the completed run in the same synchronous update as the selection; a rejected load leaves readiness null, so Run/Save/Export stay disabled instead of silently reusing the previous dataset. `run()` lost its "fetch and adopt whatever comes back" fallback and `ensureCandles` no longer fetches, which closes the same hazard for the parameter sweep.
  - One monotonically increasing generation token serves dataset loads and runs, with per-kind owner refs. Every asynchronous write requires both halves: still the newest job of its kind, and the context it started for is still live. Because candles do not depend on the strategy, a strategy edit invalidates in-flight runs without orphaning an in-flight load.
  - Save/Export read strategy, dataset, interval, range, metrics, and trades from the artifact only. The strategy NAME stays live deliberately: it changes neither the result nor the durable identity (renaming after a run must not force a re-run, which `e2e/strategy-library.spec.ts` already relies on), and Save proves `buildStrategyDef(snapshot).strategy_hash === artifact.strategyHash` before writing anything.
  - Scope note: the DEV-only `mockClient` gained `?mock=1&candleDelay=<ms>` latency and `candleFailId=<dataset-id>` rejection knobs — the one file outside the spec's "Files likely affected" list, authorized by the maintainer on 2026-07-31 because the spec's deterministic late-load/failure regressions cannot exist without them. They sit behind the existing `import.meta.env.DEV` guard and are absent from production builds. Sweep-result provenance (`BUG-SWEEP-CONTEXT-001`), metric formulas (`METRIC-002`), Rust, schema, and the report schema were not touched.
  - Validation: `npm run typecheck`, `npm test` (409, +27), `npm run build`, and `npm run e2e` (29, +4) pass. No Rust files changed, so `cargo` was not re-run. Manual check: sample -> run -> edit fee -> metrics/Save/Export disappear behind the 已失效 notice -> re-run -> all return with matching metadata.

- [x] **RUNNER-EXEC-001** — fixed CPU worker pool, Tauri lifecycle commands, single SQLite coordinator/writer, checkpointed progress, and versioned events (completed locally 2026-07-30 on `feat/runner-exec-001`).
  - Added the backend coordinator and fixed-size standard-library worker pool. Workers receive immutable inputs and return owned Train/Validation assessment bundles only; they never receive SQLite or Tauri handles. The coordinator claims paired jobs, runs the params-only split/embargo/backtest/benchmark/Random Entry/Gate→Score pipeline, and commits each candidate through the existing one-transaction store path. Hidden Test candle values are excluded from timestamp validation and all evaluation compute.
  - Replaced the discovery command stubs with `start_discovery`, blocking-acknowledgement `pause_discovery`, explicit `resume_discovery`, `cancel_discovery`, progress lookup, and recovered-run discovery. Pause stops dequeue and drains in-flight work before persisting `paused`; cancellation/failure commits first and discards late results; startup repair only pauses/requeues and never starts CPU work.
  - Added `discovery-event-v1` payloads on `discovery://progress`, `discovery://result`, and `discovery://done`, targeted to the main window through a least-privilege capability. Candidate result/progress and terminal events are emitted only after their database commits; delivery failure never rolls back committed evidence.
  - Closed the carried acceptance risks: removed the store module's `#![allow(dead_code)]` and surfaced truly unused helpers; validates evaluated candle timestamps before the infallible metrics UTC conversion; persists a JS-safe-but-Chrono-invalid input as a failed run with `error_message` and no panic/success event; validates `testedCombinations` at the JS safe-integer boundary; and locks cancel/fail late-result discard plus pause/resume ordering with coordinator tests.
  - Added `docs/discovery-runner-exec-contract.md` and reconciled the store contract. Cross-run result reuse and all typed frontend/UI work remain out of scope for their existing follow-up tasks.
  - Validation: `npm run typecheck`; `npm test` (382); `npm run build`; `cargo check --locked`; `cargo test --locked` (123); targeted `rustfmt --check`; and `git diff --check` pass. `cargo clippy --locked --all-targets` adds no runner warnings; the four pre-existing `backtest.rs`/`score.rs` warnings remain unchanged. Playwright is unchanged at 25 because this slice adds no frontend UI.

- [x] Legacy PWA UI port — the listed slices, through Slice 10 (closed 2026-07-30).
  - Scope note: this closes the slice list below, NOT full legacy parity. The remaining gaps are tracked as PARITY-001/002/003 above plus the Deferred/Optional entries (paper-live forward test, walk-forward, alerts). Closed because every listed slice is complete and an umbrella that cannot finish in one session must not sit in In Progress indefinitely (AGENTS.md §9).
  - Reality check: `AlphaFactorForge.dc.html` is a custom "dc"-framework export (`{{ }}` bindings, `<sc-for>`/`<sc-if>`, runtime `support.js`), ~1500 lines; app logic + initial state live in the `<script type="text/x-dc">` block (line ~685+). This is a REWRITE in React, not a copy-paste port.
  - Constraints found: webview CSP is `default-src 'self'`, so live exchange `fetch` from the frontend is blocked (the legacy ran as a plain PWA); SQLite currently holds no candles. Data must arrive via import (file/JSON) or a future backend fetch command.
  - Rules: reuse `src/core/*` + `tauri-client`; persist via one `metricsToBacktestSummary()` helper (PR #1 decision); code mode stays manual-only; do NOT mass-port — one small slice per PR.
  - Legacy defaults to mirror (from the script block): symbols `BTCUSDT…` + intervals `1m..1d`; `defStrat()` params (fastMA 9 / slowMA 21 / rsi 14 / fee 0.05% / slip 0.02% / size 100% / fill close / long); 6 preset strategies; localStorage keys `cd_strat` / `cd_stratlib` / `cd_paper`.
  - **Slice plan (small, one PR each):**
    - [x] Slice 1: backtest pipeline service in `src/services/` (`strategy.ts` / `strategySignals.ts` / `backtestRunner.ts` / `metricsMapper.ts`) — params-mode strategy -> entry/exit signals (12 of 14 legacy signals; `stoch*` await a core STOCH indicator) -> `runBacktest` -> `metrics` -> `metricsToBacktestSummary`. 8 unit tests; `npm test` 33/33 + typecheck green. No UI.
    - [x] Slice 2: Backtest panel UI (params mode) — `src/components/BacktestPanel.tsx` + app shell in `main.tsx`. Dataset picker (SQLite) + JSON/sample candle import, strategy params form (12 signals + exec model), run via Slice 1 service, metrics table, save (strategy_def + backtest_summary via `metricsToBacktestSummary`). Helpers: `candleAdapter` (db↔core candle), `sampleData` (seeded synthetic, CSP-safe), `strategyRecord` (StrategyDef + hash). +7 tests (40/40), typecheck + build green. Removed the PR #1 bridge self-test. No chart/sweep/replay/live/library.
    - [x] Slice 3: chart canvas — `src/charts/CandleChart.tsx` (+ pure `scale.ts`, unit-tested). Candlesticks + MA fast/slow + EMA + Bollinger overlays, volume strip, RSI subpanel (30/70 guides); overlay toggles; indicators via core/indicators (computed over full series, drawn over visible window). Wired into BacktestPanel (loads candles on dataset select). Static fit-to-width; pan/zoom + trade markers deferred. +6 tests (46/46), typecheck + build green.
    - [x] Slice 4a: blocks (rule-builder) strategy mode — `mode: 'params' | 'blocks'` on the strategy; `Rule { l: OperandId, op, r }` AND-lists for entry/exit; `buildBlocksSignals` + `buildSignals` dispatcher over a generalized `evalCond` (adds `>=`/`<=`); 15 named operands from core/indicators (stoch/atr/volMa deferred). UI: 參數/積木 tabs + rule-builder rows (operand select · op · operand|const datalist) in `BacktestPanel`. `strategyRecord` type follows `strat.mode`. +4 tests (50/50), typecheck + build green.
    - [x] Slice 4b-1: code-mode safe interpreter + signals + tests (no UI/AI/eval) — `src/services/exprInterpreter.ts` (tokenizer → recursive-descent parser → restricted AST evaluator) + `buildCodeSignals` + `mode:'code'`/`entryCode`/`exitCode`. Whitelist: ops `+ - * / > < >= <= == != && || !` + parens; variables = blocks operands; functions `prev(x)` (1-bar), `crossUp(a,b)`, `crossDown(a,b)`; finite numeric literals only. Rejects member access/indexing/assignment/strings/ternary/unknown ids+calls/non-finite; caps source ≤1000, nodes ≤128, depth ≤16, no nested time-shift. No `eval`/`Function`; AI never reaches code mode. +37 tests (87 total), typecheck + build green.
    - [x] Slice 4b-2: code-mode UI — `BacktestPanel` 參數/積木/程式碼 mode tabs + a `CodeField` (entry/exit textareas with live interpreter validation + red error border) + whitelist variable/function hint + manual-only note. No backtest-logic change (4b-1 already wired `buildCodeSignals`/dispatch). typecheck + `npm test` 87 + build green.
    - [x] Slice 5a: holdout (out-of-sample) comparison — `BacktestPanel` Holdout toggle + split % (last N% = out-of-sample). `run()` reuses `runParamsBacktest` `from`/`to` to backtest in-sample [0,split) and out-of-sample [split,n) over the same candles (full indicator history); metrics table gains 全期 / 樣本內 / 樣本外 columns. Save still uses the full-period result. typecheck + `npm test` 87 + build green.
    - [x] Slice 5b-1: parameter-sweep engine + tests (no UI) — `src/services/paramSweep.ts`. Vary 1–2 numeric params (`SWEEP_PARAM_KEYS`: fastMA/slowMA/emaPeriod/rsiPeriod/rsiBuy/rsiSell/macdFast/macdSlow/bbPeriod) over min/max/step ranges, run `runParamsBacktest` per combo, score by a `SweepMetricId` (`net`/`sharpe`/`pf`/`winRate`/`calmar`/`dd`, dd stored as -maxDrawdown). Mirrors legacy runSweep: axis cap 64, combo cap 256 (throws), best requires trades>0, pf/calmar non-finite guards (Inf→99); reuses `from`/`to` to sweep a sub-range. Pure/deterministic; a single failing combo yields a null cell. +16 tests (103 total), typecheck + build green. No UI/AI/eval.
    - [x] Slice 5b-2: parameter-sweep UI — a collapsible 「參數掃描」 section in `BacktestPanel` (X param + min/max/step, optional 2-D Y, optimisation metric; live combo-count + dup/over-cap guards). Runs the 5b-1 `runParamSweep` (yields a frame so 「掃描中…」 paints), renders a red→yellow→green `SweepHeatmap` (value + trade count per cell, best cell outlined) and 「套用最佳」 which patches the strategy. data-testid hooks (`sweep-toggle`/`sweep-2d`/`sweep-combos`/`run-sweep`/`apply-best`/`sweep-best-cell`) + a new `e2e/sweep.spec.ts` flow. No backtest-logic change. typecheck + `npm test` 105 + build + e2e (2 specs) green.
    - [x] Slice 5b-3: interactive sweep heatmap — fix the 2-D layout overlap (axes now render as full-width wrap-safe rows; metric/2-D/combo count sit in their own controls bar), make **every heatmap cell clickable** to apply that combo (`applySweepCombo`), and add an **applied-cell highlight** (blue ✓ ring via `appliedCell` state) distinct from the best-cell ★ outline; `套用最佳` reuses `applySweepCombo`. **Also highlights the applied params in the strategy form + chart quick row** (blue ✓ accent via `appliedKeys`; a param drops out the moment it is hand-edited). data-testid: per-cell `sweep-cell-<x>[-<y>]`, `sweep-best-marker`, `sweep-applied-marker`, form `applied-<key>` / chart `quick-applied-<key>`. +1 e2e (cell-click apply + form highlight) and updated 2 existing specs. typecheck + `npm test` 105 + build + e2e (4 specs) green.
    - [x] Slice 5d: chart buy/sell trade markers — draw entry/exit markers on `CandleChart` from the latest backtest `result.trades`, like a trading terminal: buy ▲ below the low (green), sell ▼ above the high (red); LONG = buy@entry/sell@exit, SHORT flips. Pure `tradeLegs(trades, timeToIndex)` in `scale.ts` (maps trade `entryTime`/`exitTime` → bar index + buy/sell, drops unknown times); a new `trades` overlay toggle (default on) in `OverlayToggles`. +4 tests (109 total), typecheck + build + e2e (4 specs) green. Canvas pixels aren't E2E-assertable; geometry is unit-tested.
    - [x] Slice 5c: clickable "?" help markers — reusable `src/components/HelpTip.tsx` (small circular `?` that toggles a short explanation popover; closes on click-again / Escape / outside-click; `align` left|right anchor avoids container-edge overflow; `role="tooltip"` + `aria-label`/`aria-describedby`, `type="button"` + stopPropagation so it never triggers an enclosing control). Wired via a central `HELP` copy map onto the 資料集 / 策略 / 執行模型 / Holdout / 回測績效 / 參數掃描 headers and the 執行回測 / 儲存結果 / 執行掃描 / 套用最佳 actions. The Holdout row was rewrapped so the tip sits outside its `<label>` (can't toggle the checkbox). UI-only; no logic/backtest change. +3 e2e (`e2e/help.spec.ts`: toggle · Escape+outside-close · single-open-at-a-time); typecheck + `npm test` 109 + build + e2e (7 specs) green.
    - [x] Slice 6: bar replay + live signals — step through candles bar-by-bar to watch the strategy trigger. Done across 6-1/6-2/6-3:
      - [x] Slice 6-1: replay cursor on the chart (no autoplay) — pure `replayWindow(total, upto, maxBars)` in `scale.ts` (inclusive [start,end] window ending at the cursor; `upto=null` keeps the pre-replay latest-`maxBars` behaviour) drives a new optional `upto` prop on `CandleChart` (clips every pane + trade markers to [start,end]; indicators still computed over the full series; dashed blue playhead at the cursor). `BacktestPanel` gains a 「回放模式」 toggle + ⏮/◀ scrubber(range)/▶ + 「第 i/n 根」 readout (cursor clamps to the latest bar whenever candles change). +4 unit tests (113 total) + `e2e/replay.spec.ts` (step · scrub · reset · hide). typecheck + build + e2e (8 specs) green.
      - [x] Slice 6-2: autoplay — ⏵/⏸ play + a 1×/2×/4× speed select driving the 6-1 cursor via a `setInterval` (400/speed ms). Pure `replayTick(cursor,total)` in `scale.ts` (advance one bar, clamp, `atEnd`) does the step; a separate effect stops autoplay at the last bar (kept out of the state updater → StrictMode-safe); play from the end restarts at bar 0; autoplay stops when 回放模式 is turned off or candles change. +2 unit tests (115 total) + `e2e/replay.spec.ts` autoplay flow (⏵ → advances → auto-stops at 600/600). typecheck + build + e2e (11 specs) green.
      - [x] Slice 6-3: live signal readout — under the replay controls, a 「此根訊號」 row shows for the cursor bar whether 進場/出場 conditions are TRUE (via the same `buildSignals` the backtest uses, memoized over candles+strat so it's not recomputed per autoplay tick; code-mode parse errors hide the row) plus 持倉 多/空/空手 from the last backtest's trades via pure `positionAtTime(trades,t)` in `scale.ts` (inclusive bounds; '—（回測後顯示）' until a run). No live exchange fetch (CSP-blocked; replay-driven only). +2 unit tests (117 total) + `e2e/replay.spec.ts` (readout labels + position resolves after a backtest). typecheck + build + e2e (12 specs) green.
    - [x] Slice 7: strategy library + report (JSON/CSV) export. Split into small PRs:
      - [x] Slice 7-1: pure report/export formatters (no UI/IO) — `src/services/reportExport.ts`: `buildReport`/`reportToJson` (a schema-versioned JSON snapshot: app + exportedAt ISO + strategyName + full strategy + dataset meta + metrics + trades), `tradesToCsv` (header + one round-trip-trade per row, +ISO times, RFC-4180-ish quoting), `suggestedFilename` (fs-safe `AlphaFactorForge_<symbol>_<interval>_<date>.<ext>`). +5 unit tests (125 total). typecheck + build green. No UI; module unused until 7-2 wires it.
      - [x] Slice 7-2: export UI + file write — 「匯出 JSON / 匯出 CSV」 buttons on 回測績效 call the 7-1 formatters (`reportToJson` / `tradesToCsv`) and a typed `files.saveReport` wrapper. Tauri command `save_report` writes sanitized `.json` / `.csv` filenames to the OS Downloads directory and avoids overwriting existing files; dev/mock uses a browser Blob download fallback. +2 Rust helper tests + `e2e/export.spec.ts`; typecheck + `npm test` 125 + build + `cargo check --locked` + `cargo test --locked` 4 + `npm run e2e` 14 green. Playwright now uses one worker by default to avoid Windows/Vite cold-load flakes in the mock browser suite.
      - [x] Slice 7-3: strategy library — list SQLite-saved strategies through the existing `get_strategies` Tauri command + typed client; validate persisted definitions before loading them into the params/blocks/code form; refresh after save; unsupported DSL rows remain read-only in the list. Strategy name now lives with the editor so it remains visible after loading clears stale backtest results. +3 unit tests and `e2e/strategy-library.spec.ts`; typecheck + `npm test` 128 + build + cargo check/test + e2e 15 green. Replaces the prototype's localStorage `cd_stratlib`; delete remains optional/deferred.
    - [x] Slice 10 (user-requested 2026-07-01; deferred pan/zoom, low priority): chart pan/zoom in the ported app (the legacy prototype had it; the port had been static fit-to-width since Slice 3). Introduce a visible-window state and reconcile it with replay `upto` + `maxBars`. Touches `CandleChart` heavily; done after Slice 7.
      - [x] Slice 10-1: cursor-anchored wheel zoom + reset-to-fit — `CandleChart` owns an inclusive visible-bar window; negative/positive wheel deltas zoom in/out by 0.8×/1.25× around the bar under the mouse, clamped to 10–`maxBars` bars and dataset bounds. During replay, `reconcileBarWindow` preserves the zoom count while following `upto`, so future candles remain hidden and the playhead stays visible. Dataset changes and replay enter/exit reset to fit. Overlay shows 「顯示 N 根」 + a reset button. The canvas uses a native `{ passive: false }` wheel listener so zoom never scrolls the surrounding page. +5 pure scale tests (133 total) + `e2e/zoom.spec.ts` (normal zoom/reset/max-fit + page position lock + replay boundary). typecheck + build + cargo check/test + e2e 17 green.
      - [x] Slice 10-2: drag-pan — pointer-capture drag on a zoomed visible window with a 4px movement threshold, so a short press remains hover/click and only a true drag hides the crosshair. Pure `panBarWindow` preserves bar count and clamps to dataset bounds or the replay cursor; dragging right reveals older bars, dragging left reveals newer. Replay resumes follow mode when panned back to its right boundary; a historical panned window never paints a false playhead at its right edge. Canvas exposes grab/grabbing cursors plus diagnostic start/end data attributes. +3 scale tests (136 total) + `e2e/pan.spec.ts` (click-vs-drag + index shift/count preservation + replay boundary). typecheck + build + cargo check/test + e2e 19 green.
    - [x] Slice 8 (user-requested 2026-07-01): pop-out 圖表 / 回測績效 into an enlarge-able view via a button, non-modal so the other sections stay usable. **Decision 2026-07-01: do (a) now; keep (b) as a future advanced version.**
      - [x] Slice 8a: in-app floating resizable/draggable panel — reusable `src/components/FloatingPanel.tsx` (title-bar drag + bottom-right corner resize + ✕/Esc close, `position:fixed`, `role=dialog aria-modal=false`, NON-modal — no backdrop — with a render-prop giving children the inner size so the chart canvas fills it). `BacktestPanel` factors chart + metrics into `renderChart(h)` / `renderMetricsTable(fontSize)` and adds an 「放大/收合」 button on the 圖表 and 回測績效 headers; when popped the section shows a `PoppedOutNote` inline and the content renders enlarged in the panel, still driven by the same React state so left-column edits reflow live. Chart pop-out defaults over the results area so strategy controls stay clear. UI-only; no backtest/logic change. +`e2e/popout.spec.ts` (chart: open → run backtest from the still-usable left column → close; metrics: open → Esc close). typecheck + `npm test` 113 + build + e2e (10 specs) green.
      - [x] Slice 8b: real Tauri second OS windows for true multi-monitor pop-out. Split to preserve one-small-slice-per-PR:
        - [x] Slice 8b-1: chart OS window — async Rust `open_popout_window("chart")` uses a stable single-instance label, focuses an existing window, or builds resizable `index.html?window=chart` via `WebviewWindowBuilder` (async avoids the documented Windows WebView2 deadlock). `ChartPopoutWindow` mounts without the main workspace; a typed `windowBridge` ready handshake + targeted snapshot/cursor events sync dataset candles, strategy, overlays, trades, and replay while preserving child-local zoom/pan. Full candles are not resent on replay ticks. A least-privilege Tauri capability grants `listen`/`unlisten`/`emitTo` only to `main` and `chart-popout-window`. +2 TS tests (138 total), +3 Rust tests (7 total), +`e2e/native-window.spec.ts` (20 E2E total); typecheck + build + cargo check/test green. Native click/open smoke remains a PR manual checklist because the Windows Computer Use helper pipe was unavailable after the required retry.
        - [x] Slice 8b-2: metrics OS window — extracted `MetricsTable` for the inline, floating, and native views; added a standalone `index.html?window=metrics` child mount and typed ready/snapshot events that keep full/Holdout results synchronized and clear stale child results when the main result resets. Rust opens or focuses one stable `metrics-popout-window`; a separate least-privilege capability grants event access only to `main` + that window. +3 TS tests (193 total), +2 Rust tests (9 total), +1 child-route E2E (22 total); typecheck + build + cargo check/test + full E2E green. Native click/open/focus/snapshot smoke remains a PR manual checklist because the Windows Computer Use native pipe was unavailable.
    - [x] Slice 9 (user-requested 2026-07-01): chart hover crosshair + unified 「此根資訊」 readout — extends the Slice 6-3 row so pointing at ANY bar shows its info in ANY mode (not just at the replay cursor). Pure `barAtX(x,padL,plotW,start,n)` in `scale.ts` (mouse-x→bar index, clamped) + `CandleChart` reports the hovered bar via `onHoverBar` (mouse handlers read a `layoutRef` written by `draw()`, which now returns its geometry) and draws a dashed crosshair; canvas gets `cursor:crosshair` + `data-testid`. `BacktestPanel`: `hoverBar` state; `activeBar = hovered ?? (replay cursor if on)`; the row (renamed `bar-info`/`bar-position`) shows 第N根 · 開高低收·量 · 進場/出場 · 持倉, gated on `activeBar != null` so it appears on hover even without replay. +3 unit tests (120 total) + `e2e/hover.spec.ts` (hover shows row w/ OHLC, leave hides) + renamed replay readout testids. typecheck + build + e2e (13 specs) green.
  - Carry-over detail (kept from backlog): suggested folders `src/components`, `src/pages`, `src/charts`, `src/stores`, `src/services`; preserve the terminal-like dense visual style; replace prototype localStorage persistence with SQLite via `tauri-client`.

- [x] **RUNNER-STORE-001** — discovery run/job store, migration 0003, and the atomic candidate commit (PR #66 Resolution slice 7).
  - Migration `0003_discovery_runner.sql` gives `discovery_runs`/`discovery_jobs` their first data-carrying structure (both were schema-only with no writer since 0001): `discovery_jobs.candidate_index` + `UNIQUE(run, candidate, segment)` so a retry cannot duplicate a paired checkpoint; `validation_records.discovery_run_id` (nullable) + a PARTIAL unique index so runner assessments are unique per (run, strategy, dataset) while manual saves stay unconstrained and PERSIST-001's append-only behaviour is unchanged; and a partial unique index on an expression shared by every `running`/`paused` row, enforcing D5's one-non-terminal-run rule in the database rather than by check-then-act.
  - `src-tauri/src/db/discovery.rs` owns the D5 state machine (`idle→running`, `running→paused|completed|failed|cancelled`, `paused→running|cancelled`; terminal never resumes; `idle` holds no global slot) and the atomic candidate commit: ONE transaction covering both segment summaries and trades, the append-only record with its run linkage, BOTH job rows, run progress, and the strategy lifecycle. `status = 'done'` is the runner's only checkpoint, so a partial apply would make a resumed run skip work that never landed.
  - Fail-closed by construction: `Segment` is a two-variant enum so a Test job row is unrepresentable; lifecycle is derived from `record.gate_passed` and `best_strategy_id` from the stored assessments, so a caller cannot record a verdict or a winner the evidence does not support; a commit is refused unless the run is `running` and the queued pair exists and matches the committed summaries' identity. Per D6 a validated strategy is never demoted by a later failure.
  - Crash recovery pauses orphaned `running` runs, requeues only in-flight jobs, never touches `done` rows, never auto-resumes CPU work, and is idempotent.
  - Test discipline: the rollback claim is pinned by `a_failure_at_the_last_write_rolls_back_jobs_lifecycle_and_progress`, whose failure fires AFTER the writes and which asserts the surviving rows keep the PREVIOUS commit's values. The weaker "assert nothing exists" shape passes whether or not the rollback works (its guard fires before any write) and is kept separately under a name saying so. Verified by mutation: flipping the transaction drop behaviour to `Commit` fails the real test and passes the weak one.
  - Documented in `docs/discovery-runner-store-contract.md`. At STORE completion, `db/discovery.rs` carried a temporary module-level `#![allow(dead_code)]` because no non-test caller existed; completed RUNNER-EXEC-001 has now removed it and wired the store.
  - Validation: `npm run typecheck`; `npm test` (382, unchanged — no frontend surface); `npm run build`; `cargo check --locked`; `cargo test --locked` (107, +41); `cargo clippy --locked --all-targets` clean on the new files; targeted `rustfmt --check` clean; `npm run e2e` (25); `git diff --check` clean. The 4 pre-existing `backtest.rs`/`score.rs` clippy warnings remain proposed, not fixed here.

- [x] **RUNNER-CONFIG-001** — discovery run admission contract: strict config parsing, params-only candidate enumeration, and deterministic seeds (PR #66 Resolution slice 6).
  - Added the pure TypeScript reference `src/services/discoveryConfig.ts` (`discovery-config-v1`), `candidateEnumeration.ts` (`discovery-enumeration-v1`), and `discoverySeed.ts` (`seed-v1`), plus the pure Rust ports `discovery_core/{config,enumerate,seed,identity}.rs`. Nothing here reads SQLite, spawns a thread, emits an event, renders UI, or executes the hidden Test segment.
  - Admission (D4): exact key sets at every level, contract-version pinning read from the owning modules, durable `dataset-content-v2` identity, resolved-not-referenced benchmark costs that must agree with every base preset, full resolved Gate/Score configs validated with the owning modules' own messages, explicit `rootSeed` u32, `caps.candidates` in `1..=4096` (256 default), and `maxConcurrency` resolved as `max(1, logicalCores - 1)` or a `1..=logicalCores` override.
  - Candidate space (D2): params mode only — RUNNER-CONFIG is where blocks/code candidates are rejected, so the Rust engine has no non-params path. Whitelisted axes are the indicator and risk parameters; `feePct`/`slipPct`/`sizePct` are deliberately not axis-eligible so discovery cannot "win" by assuming cheaper fills. Axis values are inclusive `min + i*step` by multiplication (never accumulation), capped at 64 per axis.
  - Enumeration: the raw Cartesian product is preflighted against the cap BEFORE any candidate is built; cross-field rules (`fastMA<slowMA`, `macdFast<macdSlow`, `rsiBuy<rsiSell`) prune, canonical `strategy-v2` hashing deduplicates across bases, survivors sort by hash and are indexed afterwards, and `raw`/`prunedInvalid`/`duplicates`/`finalUnique` must reconcile. Score's `N` is derived here (`lineage-final-unique`), never accepted from config. `seed-v1` sub-seeds hash `rootSeed + datasetContentHash + strategyHash + purpose` — never a row id, thread id, or completion order.
  - Added `src/parity/runnerConfigFixture.ts` + `npm run fixtures:runner-config` + committed `runner-config-parity-v1`. Its declared policy is `expectedNumericPolicy: exact-v1` (no tolerance at all): 5 seed cases locking exact preimage BYTES and derived u32s, 6 axis cases including exact `0.30000000000000004` binary drift and the 64-value boundary, 5 concurrency cases, 2 complete resolved-config cases, 6 complete candidate plans (including a base-order-reversed twin proving declaration order changes nothing), and 70 TypeScript-held error cases (10 seed + 1 axis + 4 concurrency + 53 admission + 2 enumeration).
  - Documented in `docs/discovery-config-contract.md`; `docs/rs-core-parity.md` gained the matching parity section. Recorded follow-up: `discovery_core/identity.rs` and the binary crate's `identity` module hold two copies of the canonical encoder (crate purity forces the split); both are locked to the same committed identity fixture, and consolidation is proposed as its own change.
  - PR #73 review fixes (2 blockers + 5): (1) `slPct`/`tpPct`/`feePct`/`slipPct` moved from a `>= 0` domain to `[0, 100]` — these are divided by 100 and the engine rejects any normalized fraction above 1, so `feePct: 101` previously passed admission and was guaranteed to throw once a job executed; (2) candidates shared one mutable `entryRules`/`exitRules` array with each other and with the caller's input, so mutating one candidate changed every other candidate's content while their hashes/seeds stayed put — admission and enumeration now deep-clone, matching PERSIST-001's decoupling standard; (3) durable identities now require `<version>:<64 lowercase hex>` instead of only the version prefix, so an empty/truncated/uppercase digest can no longer seed a real random stream; (4) TypeScript now sorts unknown keys by UTF-8 bytes to match Rust's `String: Ord` (the default UTF-16 sort named a different key first for non-ASCII input, breaking this slice's own rejection-order contract); (5) the held-error total was misreported as 54 (the 2 enumeration errors were omitted) — both languages now assert the real total; (6) the admission group is locked by exact ordered ID inventory, not by count; (7) the handoff record was relocated to the end of the file, restoring append-only chronology.
  - PR #73 second-review fixes (3): (1) Rust exposed BOTH `axes` (serialized into the audit record) and `parsed_axes` (walked by the enumerator) as public fields, so a recorded config could describe a different grid from the one that produced the candidates — `DiscoveryAxis` now stores a `&'static str` key borrowed from the whitelist and is the single field both paths read; (2) the benchmark-cost percent test could never reach its branch because bases parse first and the base strategy's own 101 threw earlier — the base preset now stays valid and two fixture cases cover the resolved-cost domain directly; (3) `exact-v1` did not state its sign-of-zero semantics, and IEEE-754 `-0.0 == 0.0` would have let a sign flip pass — both languages now assert no leaf is negative zero.
  - PR #73 third-review fixes (3 + 2): (1) the `-0` guard was a FALSE NEGATIVE — it ran against the parsed artifact and against a JSON round trip, but `JSON.stringify(-0)` is `"0"`, so it could never observe one; it now runs on the live builder output with negative controls, verified by injecting `min: -0`, regenerating so the artifact matches byte for byte, and confirming it still fails; (2) the Rust axis comparison bypassed the `exact-v1` rules with a bare `==` (`-0.0 == 0.0`) — every numeric comparison now routes through one `assert_exact_leaf` helper that is itself tested to PANIC on `-0`/`NaN`/`±Infinity`; (3) `pub key: &'static str` did not mean "whitelisted" (any literal is `'static`) and the doc comment claiming it did was wrong — the key is now a closed `AxisKey` enum whose only text constructor is `parse`, making a non-whitelisted axis unrepresentable; (4) deep-clone regressions extended to `exitRules` and to nested rule-object mutation; (5) the previous correction had edited an earlier handoff record IN PLACE (`Four` → `FIVE`) while describing it as unchanged — the original wording is restored and the count recorded in a new append-only section.
  - Validation after all three review rounds: fixture regeneration SHA-256 blob-identical; `npm run typecheck`; `npm test` (382, +43); `npm run build`; `cargo check --locked`; `cargo test --locked` (66, +19); targeted `rustfmt --check` clean; `npm run e2e` (25); `git diff --check` clean. `cargo clippy --locked --all-targets` runs clean on every new file — five warnings were addressed: one adopted `manual_range_contains`, plus four `neg_cmp_op_on_partial_ord` sites carrying an explicit `#[allow]` because `!(a < b)` is the deliberate NaN-fail-closed form the TypeScript reference uses. Four pre-existing warnings in `backtest.rs`/`score.rs` are proposed, not fixed here.

- [x] **RS-CORE-005** — pure Rust Gate + Score structural parity (PR #66 Resolution slice 5).
  - Added `src/parity/gateScoreFixture.ts` + `npm run fixtures:gate-score` + committed `gate-score-parity-v1`: 6 params-only complexity cases covering all 12 supported signal ids, 22 encoded Gate cases, 4 complete Score cases, 16 Gate errors, and 11 Score errors held by the TypeScript reference. The envelope locks exact structure/order/status/evidence/error inventories, explicit non-finite/negative-zero input tags, UTC/invalid-Date boundaries, safe-integer and fractional limits, precise audit details, extreme finite contribution/sigma arithmetic, partial/default config resolution, and finite-weight aggregate overflow; only finite non-integer leaves use the declared tolerance.
  - Added pure Rust `discovery_core/gate.rs` and `score.rs` plus a five-test fixture consumer. Gate emits raw and JSON-safe encoded verdicts; Score is params-only by construction and matches complete `score-v1` breakdowns. TypeScript Gate/Score boundaries now reject duplicate benchmarks, malformed/non-finite evidence, unstable sigma intermediates, negative-zero drift, and non-finite aggregate scores before parity is frozen. No runner, SQLite, threads, events, UI, blocks/code discovery candidates, or hidden Test execution was added.
  - Validation: fixture regeneration SHA-256 is blob-identical; `npm test` (339); `npm run typecheck`; `npm run build`; `cargo check --locked`; `cargo test --locked` (47); targeted `rustfmt --check`; `npm run e2e` (25); and `git diff --check` pass. Clippy was intentionally not run.

- [x] **RS-CORE-004** — pure Rust deterministic-benchmark, mulberry32, and Random Entry parity (PR #66 Resolution slice 4).
  - Added `src/parity/benchmarkFixture.ts` + `npm run fixtures:benchmarks` + committed `benchmark-parity-v1`: 5 exact raw-u32 PRNG cases, 4 deterministic-suite cases, 2 exact planner cases, 6 Random Entry integration cases, and 8 TypeScript-held fail-closed cases. Coverage positively exercises SMA 50/200, full strategy-object structure, trades/equity/metrics and METRIC-001 statuses, unknown/prototype-key interval fallback, clipping/drop behavior, `bars: 0→1`, subranges, default 200 runs, strict percentile ties, and accepted/rejected seed/run boundaries.
  - Added pure Rust `discovery_core/benchmarks.rs`, `prng.rs`, and `random_entry.rs`, with exact benchmark strategy audit records and numeric-leaf tolerance comparisons against the TypeScript reference. Shared TS/Rust parity encoders/comparators now prevent the backtest and benchmark fixture paths from drifting; source provenance includes the shared non-finite codec. No runner, SQLite, threads, events, UI, or hidden Test execution was added.
  - CI portability fix: fixture-only rising paths use iterative multiplication instead of platform-dependent `Math.pow`, preserving exact fixture freshness across Node/OS runtimes without weakening exact inputs or the declared Rust float tolerance.
  - Validation: both fixtures regenerate blob-identically; `npm run typecheck`; `npm run test` (324); `npm run build`; `cargo check --locked`; `cargo test --locked` (42); targeted `rustfmt --check` and `git diff --check` clean; Playwright untouched (no UI/mock surface).

- [x] **RS-CORE-003** — pure Rust params-signals, validation-split, and embargo-derivation parity (PR #66 Resolution slice 3).
  - Added `src/parity/signalsSplitFixture.ts` + `npm run fixtures:signals-split` + committed `signals-split-parity-v1` envelope: 7 signal cases (hand-verified exact MA-cross index + one sample case per family, locking bar-0-never-signals and warm-up-NaN-false), 9 split cases (all five usable-bar residues, zero/non-zero embargo, the JS safe-integer extreme), 8 embargo cases (per-family lookbacks, holding allowance, unused-period usage-awareness, and a success case landing embargoBars exactly on MAX_SAFE_INTEGER), and 11 error cases HELD by the TS reference (generation + freshness execute the real TS functions and require the recorded fragment). Every EXPECTED OUTPUT leaf compares exactly (signal booleans; split/embargo integers — inputs still carry floats); both languages assert the exact success AND error inventories by id.
  - Added pure Rust `discovery_core/signals.rs` (`params-signals-v1`), `split.rs` (`validation-split-v1`), and `embargo.rs` (`embargo-derivation-v1`); per D2 only params mode is ported — blocks/code and the expression interpreter stay TypeScript-only; RUNNER-CONFIG owns non-params mode rejection (recorded), and unsupported ids fail closed with the identical message.
  - PR #70 review fixes: TS `safeLookback` checked arithmetic on every derived lookback and the final embargoBars (IEEE-754 would silently round past MAX_SAFE_INTEGER where i64 would not); Rust rejects raw periods above the safe range BEFORE `usize→i64` conversion (previous `as i64` could wrap) and uses bounded checked adds throughout; boundary cases lock raw-period-above-range, derived-lookback overflow (the reviewer's RSI reproduction), allowance-sum overflow, and the exact-MAX_SAFE success; handoff chronology corrected via append-only record.
  - PR #70 third-round fix: post-hoc `isSafeInteger` cannot catch an intermediate rounding cancelled by later subtraction (`a + b - 1`), so every derived addition now goes through a PRE-checked `safeAdd` and the MACD composite is reassociated as `slowest + (signal − 1)`; the reviewer's blocks `macdHist > 0` reproduction and its code-mode twin are locked as TS unit regressions (blocks/code stay TS-only, outside the Rust fixture).
  - Validation: `npm run typecheck`; `npm test` (321); `npm run build`; `cargo check --locked`; `cargo test --locked` (38); targeted `rustfmt --check` clean; fixture regeneration blob-identical; e2e untouched (no UI/mock surface).

- [x] **RS-CORE-002** — pure Rust backtest engine and metrics parity (PR #66 Resolution slice 2).
  - Added `src/parity/backtestFixture.ts` + `npm run fixtures:backtest` + committed `backtest-parity-v1` envelope: 20 behaviour cases (long/short/both × close/nextOpen, same-bar exit+entry, `both` simultaneous-signal entry-wins, SL/TP incl. gap-through and SL-first ambiguity, fee-budgeted 100% sizing, EOD settlement, from/to sub-range and from==to, the Resolution's empty boundaries — empty candle series and inverted from/to evaluating no bar — zero trades, no-downside +Infinity Sortino/Calmar/PF, and two 180-day sample cases spanning ≥4 calendar months with risk exits) plus 3 fail-closed config error cases. Every case's expected output comes from the real TS engine with generation-time sanity invariants; per the PR #69 review, the error cases are HELD by the TS reference (generation + freshness both run them and require a fragment-matching RangeError) and both languages lock the exact 20-case inventory by id.
  - Added pure Rust `discovery_core/backtest.rs` (`backtest-execution-v1`: fee-inclusive entry budgeting, 1× short collateral, nextOpen pending fills, gap-aware SL-first risk exits, legacy `both` reversal, settled EOD endpoint, identical fail-closed messages) and `discovery_core/metrics.rs` (`metrics-v1`: METRIC-001 downside deviation over all bars, +Infinity Sortino/Calmar/PF, UTC monthly returns). No Tauri/SQLite/thread/event/UI dependency; Test never executes.
  - Rust parity locks trades (times/side/bars exact; prices/pnl within the declared tolerance), full equity curves, all metric leaves incl. exact non-finite statuses and monthly-return keys, and the 3 error messages. Fixture regeneration is blob-identical across runs; the vitest freshness test rebuilds and deep-equals the envelope.
  - Validation: `npm run typecheck`; `npm test` (315, +4); `npm run build`; `cargo check --locked`; `cargo test --locked` (34, +2 parity suites); targeted `rustfmt --check` clean on the new discovery_core files; e2e untouched (no UI/mock surface).

- [x] **RS-CORE-001** — TypeScript-reference parity harness foundation plus pure Rust candle/types and indicator parity.
  - Added an explicit deterministic fixture generator and committed `indicator-parity-v1` envelope with source SHA-256 hashes, exact warm-up positions, structural JSON rules, and the PR #66 default float tolerance. Synthetic sample candles are fixture input only.
  - Added a Tauri/SQLite-free Rust `discovery_core` library matching SMA/EMA/WMA/RSI/MACD/TR/ATR/Bollinger/stddev/extrema/ROC output against the same fixture; runner/DB/thread/event/UI work remains excluded.
  - Fixture SHA-256 is stable across regeneration with canonical `utf8-lf-v1` source hashing; 311 vitest + typecheck + build + rustfmt check + cargo check + 32 Rust tests + 25 Playwright e2e pass.

- [x] **IDENTITY-001** — durable identity prerequisite for discovery reuse.
  - Added SHA-256-only `strategy-v2` and `dataset-content-v2` identities over explicit cross-runtime binary encodings. Dataset identity includes the versioned field mapping, metadata, bounds/count, and every timestamp-sorted OHLCV value; FNV is explicitly `ephemeral-fnv1a` only.
  - Rust now recomputes strategy and dataset identities at the product write boundary. Dataset row + strict candle inserts commit in one transaction; identical re-imports reuse the row, while legacy/forged hashes, contradictory payloads, duplicate timestamps, non-finite values, and injected candle failures leave no partial write.
  - Committed one exact TypeScript/Rust fixture and the durable contract document. `npm run typecheck`, 308 Vitest, production build, `cargo check --locked`, 28 Rust tests, and all 25 Playwright e2e tests pass; no migration was required because versioned values fit the existing hash columns and legacy rows remain read-only/ineligible for discovery reuse.

- [x] PERSIST-001 — immutable validation-record persistence (implements the PR #64 handoff Resolution, revised Option C + DB-invariant addendum).
  - Migration `0002_validation_records.sql`: append-only audit table with FKs, the `gate_passed IN (0,1)` CHECK, and the addendum's D3 invariant CHECK (gate fail ⇒ score NULL, pass ⇒ NOT NULL); registered in the migration runner; fresh-apply, 0001→0002 upgrade-preserving-data, and idempotent re-run are test-locked. `discovery_runs` remains untouched (input-only batch config per the Resolution).
  - TS: shared `services/metricsCodec.ts` (single codec extracted from reportExport; `assertJsonSafe` recursively throws on any unencoded non-finite before serialization) + `services/validationRecord.ts` (`buildBenchmarkRecord` metrics-only snapshot with full Random Entry evidence, `encodeGateVerdict` with `valueStatus`, `AssessmentOutcome` discriminated union making "gate failed but scored" unrepresentable, `buildValidationRecord` self-contained `validation-record-v1` envelope with contract versions, `buildValidationBundle` producing D3-conformant Train/Validation summary rows + record row). Typed `db.saveValidationRecord` / `listValidationRecords` / `getValidationRecord` wrappers with mock-client parity (same pre-validation + append semantics).
  - Rust: `ValidationRecordRow` DTO, pre-transaction `validate_validation_bundle` (segments, identity, gate/score nullability incl. finite score, Train Phase-B nulls, envelope-version match), atomic `save_validation_bundle` (Train + Validation summaries/trades + record in ONE transaction; `write_backtest_result` extracted so the existing single-save keeps its own transaction), and newest-first list/get. Rollback-after-partial-write, append-on-rerun, CHECK/FK enforcement, and exact record JSON read-back are test-locked.
  - Documented in `docs/validation-record-contract.md`. Out of scope per the Resolution: UI wiring, lifecycle transitions, discovery runner, Test persistence.
  - PR #65 review fixes (3 blockers): (1) snapshots now deep-decouple from every caller input (`deepSnapshot`, cloned `monthlyReturns`, rebuilt strategy/plan/score/benchmark) and `toJsonSafeString` re-runs the recursive non-finite guard immediately at EVERY stringify boundary — post-build injected Infinity/NaN fails closed instead of silently persisting null, locked by mutation-isolation + injection regressions; (2) the Rust validator now requires the Validation summary score to be finite AND equal to the record row, and parses the envelope to enforce identity/gatePassed/score equality plus structural breakdown/benchmark snapshot equality with the summary's latest view (key-order differences tolerated), locked by a contradictions rejection test; (3) the mock runs the shared `assertValidBundle` (TS mirror of the Rust validator), returns detached rows from list/get, and gained `mockClient.test.ts` covering illegal bundles + append/read immutability.
  - PR #65 second-review fixes (2 residual P2): the TS validator now uses true JSON structural equality (`jsonStructuralEqual`, key order irrelevant / array order significant — matching `serde_json::Value` semantics, with a key-order-only mock pass regression), and BOTH validators lock a `bench-record-v1` shape (non-null object + exact version + benchmarks array + randomEntry object) so JSON null / `{}` / non-objects / wrong versions can never impersonate the required benchmark evidence, even when summary and envelope agree — rejection regressions on both sides.
  - Validation: `npm run typecheck`; `npm test` (300, +16); `npm run build`; `cargo check --locked`; `cargo test --locked` (21, +8); full `npm run e2e` (25) re-run after each mock-seam change.

- [x] SCORE-001 — §5.2 score-v1 ranking breakdown (pure service, no UI; implements the PR #61 handoff Resolution).
  - Added `alpha-factor-forge/src/services/score.ts`: `scoreCandidate` computes the unclamped weighted sum `Σ(components) − Σ(penalties)` over a Validation-segment result with fixed recorded normalization (D1), returning a fully JSON-safe breakdown — every entry `{ id, raw, rawStatus, normalized, weight, contribution, evidence? }` with `rawStatus ∈ finite | positive_infinity | insufficient | invalid | deferred`, plus `formulaVersion: score-v1`, `segment: validation`, the resolved config, and the lineage-final `testedCombinations` evidence (D5).
  - Components: CAGR/1.0, Sortino/5, Calmar/5, PF (pf−1)/2 (legitimate +Infinity → normalized 1), revised Consistency `1/(1+10σ)` with population σ and a 3-finite-month floor (D3); the regime component is a deferred placeholder whose non-zero weight throws until REGIME-001 (D2). Penalties: canonical cross-mode `complexityUnits` (decision nodes + distinct active indicator params + enabled SL/TP; MA-cross parity locked at 8 units across params/blocks/code), turnover proxy `closedTrades/totalBars@v1` with provenance evidence, and `clamp01(log10(N)/4)` data-mining with `lineage-final-unique` basis (D4).
  - Fail closed: invalid caps/weights/N throw `RangeError`; NaN/−Infinity → `invalid`; <3 months → `insufficient`; score always finite; only `ValidationRunResult.validation` is read (test-locked with throwing Train/Test getters). Documented in `docs/score-contract.md`.
  - 13 acceptance tests mirroring the reviewer checklist, including a hand-computed 2.65 baseline and a JSON round-trip lock.
  - Validation: `npm run typecheck`; `npm test` (284); `npm run build`; `e2e/` greps clean (module unwired, no UI surface).

- [x] METRIC-001 — core metrics correctness mandated by the SCORE-001 Resolution (Next → In Progress → Done in one session).
  - `core/metrics`: downside deviation is now `sqrt(mean(min(0, excess)^2))` over ALL bar returns, so a single downside observation yields a finite positive Sortino instead of 0; Sortino = `+Infinity` only when downside is 0 with positive mean excess; Calmar = `+Infinity` when drawdown is 0 with positive CAGR; every other zero-denominator case stays 0. Sharpe unchanged.
  - Explicit non-finite handling at JSON boundaries: new `services/nonFinite.ts` (`positive_infinity` / `negative_infinity` / `nan`, matching the Resolution's `rawStatus` vocabulary); the JSON report bumps to schema 2 with finite-or-null metric values plus a `metricsNonFinite` status map — never relying on `JSON.stringify`'s silent Infinity→null. The SQLite mapper's explicit finite→null narrowing is documented and test-locked (nullable REAL columns; DB status columns deferred to a future schema slice).
  - No golden updates were needed: goldens lock Sharpe (unchanged), not Sortino/Calmar; the sweep's existing Inf→99 guard already covers the now-reachable infinite Calmar.
  - +7 tests (6 metric locks incl. the four reviewer-mandated cases + report status round-trip; extended the mapper null-narrowing lock).
  - Validation: `npm run typecheck`; `npm test` (271); `npm run build`.

- [x] GATE-001 — §5.1 hard elimination gate (pure judgment, no UI).
  - Added `alpha-factor-forge/src/services/gate.ts`: `evaluateGate` judges a candidate segment result plus the complete §6 benchmark outputs against explicit thresholds and returns a full per-criterion verdict (fixed §5.1 order, observed value + threshold + pass, and the exact config) for reproducible recording. It runs no backtests.
  - §5.1 defaults recorded in `DEFAULT_GATE_CONFIG`: trades ≥ 30, cost-adjusted avg trade > 0 (strict), rolling 30-bar positive-window ratio ≥ 55% (window length is a recorded v1 convention), MaxDD ≤ 35%, UTC-monthly profit contribution ≤ 40%, single-trade contribution ≤ 25%, strictly beat all four deterministic benchmarks (ties lose), Random Entry percentile ≥ 95.
  - Missing evidence fails closed with `value: null` (equity shorter than one window; non-positive total profit makes concentration unverifiable); a missing benchmark or invalid config throws. Documented in `docs/gate-contract.md`; segment-length-adjusted minTrades deferred.
  - 7 focused tests: clean pass + fixed order, each criterion failing independently, strict benchmark ties, fail-closed evidence, threshold overrides, and structural throws.
  - Validation: `npm run typecheck`; `npm test` (264); `npm run build`.

- [x] BENCH-002 — Random Entry Monte Carlo benchmark (pure service, no UI).
  - Added `alpha-factor-forge/src/services/randomEntry.ts`: `runRandomEntryBenchmark` simulates N seeded runs that place the candidate's closed-trade count at random non-overlapping segment positions, holding periods sampled with replacement from the candidate's own `bars` (clamped ≥ 1), executed by the real engine long-only / 100% sizing / close fill with inherited costs; returns the per-run `netReturn` distribution + the candidate's strictly-beaten percentile. One `mulberry32(seed)` stream with a fixed consumption order; runs default 200, capped 1000; `planRandomTrades` exported for direct placement tests.
  - No pass/fail verdict — the §6 "≥ 95th percentile" threshold belongs to the Gate slice. Fail closed on empty series/segment, zero candidate trades, invalid runs/seed. Clip/drop conventions recorded in `docs/benchmark-suite-contract.md` (BENCH-002 section); `mulberry32` is now exported from `sampleData.ts` so the workspace keeps exactly one PRNG.
  - 9 focused tests: hand-verified placement (back-to-back, clip, drop), seeded invariants, determinism + seed sensitivity, default run count, percentile extremes, paired cost effect, fail-closed inputs, and an end-to-end real-candidate ranking. This completes the §6 benchmark set.
  - Validation: `npm run typecheck`; `npm test` (257); `npm run build`.

- [x] BENCH-001 — deterministic benchmark suite (pure service, no UI).
  - Added `alpha-factor-forge/src/services/benchmarks.ts`: `runDeterministicBenchmarks` runs the four `STRATEGY_DISCOVERY.md` §6 deterministic baselines — Buy & Hold (hand-built signals: enter first tested close, hold to the engine's EOD settlement), SMA 50/200 cross, RSI 14 30/70 reversion, Bollinger 20/2 mean reversion — over one candles × segment in a fixed order through the existing pipeline.
  - Fairness conventions recorded in `docs/benchmark-suite-contract.md`: benchmarks inherit the candidate's fee/slippage but always run long-only, 100% sizing, close fill, no SL/TP; segment restriction uses the engine's inclusive `from`/`to`; an empty series fails closed.
  - Random Entry Monte Carlo is deferred to BENCH-002 (needs matched holding-period distribution, run count, seed, and percentile conventions); Gate comparison rules are a later slice — this slice only produces per-benchmark metrics.
  - 8 focused tests: doc-definition lock, hand-calculated Buy & Hold (full range, sub-segment, cost effect), fixed suite order, pipeline parity for the signal benchmarks, determinism, and empty-input failure. Also restored the `## In Progress` board header dropped by the VAL-003 PR.
  - Validation: `npm run typecheck`; `npm test` (248); `npm run build`.

- [x] VAL-003 — usage-aware embargo derivation for the validation split.
  - Added pure `alpha-factor-forge/src/services/embargo.ts`: `deriveEmbargoBars(strat, holdingAllowanceBars)` returns `embargoBars = maxSignalLookbackBars + holdingAllowanceBars` with a recordable breakdown; `maxSignalLookbackBars` counts only the indicators the active mode's entry/exit signals actually reference (params signal ids, blocks rule operands, or the code expressions' interpreter-validated ASTs), using each core indicator's real warm-up (`sma`/`ema`/`bbands` → p, `rsi` → p+1, MACD signal/hist → max(fast, slow) + signal − 1) and adding one bar for `prev`/cross semantics.
  - Fail closed: unsupported `stoch*` signals, invalid code expressions, non-positive used periods, and a negative or non-integer allowance throw; unused configured periods never inflate the embargo or throw.
  - Documented the conventions in `docs/validation-split-contract.md` (new "Embargo derivation (VAL-003)" section). The module stays unwired until a later slice records the breakdown with a validation run; no UI, persistence, ranking, or Rust changes.
  - 14 focused tests across params/blocks/code modes, usage-awareness, fail-closed inputs, and `planValidationSplit` integration sanity.
  - Validation: `npm run typecheck`; `npm test` (240); `npm run build`.

- [x] VAL-002 — run the split plan through the existing backtest pipeline (Train + Validation only).
  - Added pure `alpha-factor-forge/src/services/validationRun.ts`: `runValidationBacktests` plans `planValidationSplit(candles.length, embargoBars)` and backtests the Train and Validation inclusive ranges via the existing `runParamsBacktest` `from`/`to` (full indicator history — the same causal pattern as Holdout; embargo bars are never evaluated).
  - Hidden-Test discipline preserved: the Test segment is planned but never backtested; `ValidationRunResult` exposes only `plan`/`train`/`validation`, and invalid input fails closed in the planner before any backtest runs.
  - 6 focused tests cover exact planned ranges, embargo-bar exclusion, parity with direct `runParamsBacktest` calls over the same ranges, determinism, the deliberately absent `test` field, and fail-closed inputs. No UI, persistence, ranking, or Rust changes; the module stays unused until a later slice wires it into discovery or UI.
  - Validation: `npm run typecheck`; `npm test` (226); `npm run build`.

- [x] VAL-001 — deterministic Train/Validation/Test + embargo split contract.
  - Added a pure `src/core/validation/split.ts` planner with inclusive `from`/`to` ranges compatible with the existing backtest boundary contract; no React, DOM, IO, persistence, or runner dependency.
  - V1 first excludes two equal caller-supplied embargo gaps, then allocates usable bars at fixed 60/20/20 ratios with exact integer largest-remainder math and deterministic Train→Validation→Test ties.
  - Invalid/insufficient input fails closed; 24 focused tests cover exact ranges, all five remainder residues, zero/nonzero embargo, total coverage, invalid inputs, and `Number.MAX_SAFE_INTEGER` precision.
  - Added `docs/validation-split-contract.md`; Test remains unwired from generation, tuning, ranking, prompts, UI, and hidden-Test reveal flows.
  - Validation: `npm run typecheck`; `npm test` (220); `npm run build`. Independent review closed one safe-integer precision blocker and approved the corrected implementation with blocker 0.

- [x] SEC-002 — Upgrade the development toolchain to the minimum patched Vite/Vitest lines.
  - Exact-pinned `vite@6.4.3` and `vitest@3.2.6`; the reviewed lockfile resolves `esbuild@0.25.12`, `@vitest/mocker@3.2.6`, and `vite-node@3.2.4` while retaining the compatible `@vitejs/plugin-react@4.7.0` peer.
  - Used an explicit targeted install only—no `npm audit fix`, forced upgrade, dependency override, or product source change.
  - Updated the audit report, README status in three languages, and CHANGELOG; full and production-only audits both report zero.
  - Validation: `npm run typecheck`; `npm test` (196); `npm run build`; `npm run e2e` (25); dependency tree has no invalid peers; a short-lived Vite smoke bound only to loopback (`::1`).

- [x] SEC-001 — Review npm audit findings without forced breaking upgrades.
  - Added `docs/security-audit-npm.md` with a reproducible lockfile/runtime snapshot, all five affected package-node paths, the five underlying advisories, exposure preconditions, patched floors, and an actionable classification.
  - All findings are dev-only; `npm audit --omit=dev --json` reports zero. Every affected node is `needs-window` because a complete fix crosses the current Vite 5 / Vitest 2 majors.
  - Recommended a separate minimum-patched upgrade (`vite@6.4.3` + `vitest@3.2.6`) and retained the ban on automatic/forced audit fixes. `package.json` and `package-lock.json` are unchanged.
  - Validation: full audit reproduced 3 moderate / 1 high / 1 critical; production-only audit 0; `npm run typecheck` passed; dependency-file diff is empty.

- [x] Reconcile the legacy prototype-issue carry-over before selecting new work.
  - Dataset changes already reload candles, so RSI refreshes from the new series; chart drawing already derives MA periods from current strategy state.
  - Replay and hover use one active-bar index across candle, signal, and position readouts, with unit and browser regressions covering boundaries and alignment.
  - Service Worker work remains intentionally deferred because Tauri is the target architecture; the legacy PWA is a read-only behavior reference.
  - This was a status reconciliation only; no product or test files changed.

- [x] FEAT-002 — Persist trade-detail rows with each saved backtest summary.
  - Added one typed `ClosedTrade` → `TradeRow` mapper and passed the rows through the existing Tauri client boundary.
  - Rust now upserts the summary, deletes prior child rows, and inserts the replacement trades in one transaction; regression tests cover replace, partial-insert rollback, and strategy-delete cascade semantics with foreign keys enabled.
  - Kept migration `0001_init.sql` unchanged: holding bars are not stored, per-trade fee/slippage remain `NULL`, and a trades-reading UI stays deferred to Results Explorer.
  - Validation: `npm run typecheck`; `npm test` (196); `npm run build`; `cargo check --locked`; `cargo test --locked` (13); `npm run e2e` (25).

- [x] Complete the planned browser E2E flows for BacktestPanel.
  - The dev-only `dataClient` mock seam now supports durable Chromium coverage without routing production around typed Tauri clients or storing product data in browser localStorage.
  - [x] Holdout stale-column reset — `e2e/holdout.spec.ts`.
  - [x] Invalid/valid code-mode feedback and Run guard — `e2e/code-validation.spec.ts`.
  - [x] Params/blocks/code tab-state preservation plus a blocks-mode run — `e2e/strategy-modes.spec.ts`.
  - [x] Save-result success banner through the mock persistence path — `e2e/save-message.spec.ts`.
  - Browser E2E validates frontend interaction only; Rust command wiring, SQLite persistence, migrations, AppData paths, and Tauri/WebView behavior remain owned by Rust integration tests and native smoke checks.
  - `npm run typecheck`, 193 vitest, production build, the focused save flow, and all 25 Playwright e2e tests pass. This test-only completion changes no product behavior, so no CHANGELOG or manual checklist is required.

- [x] Browser E2E flow — preserve params/blocks/code tab state across switches.
  - Added one browser/mock regression that verifies all three `aria-pressed` states, mode-exclusive controls, retained params/rule/code edits after repeated unmounts, dataset-aware Run availability, and one successful blocks-mode backtest.
  - `npm run typecheck`, 193 vitest, production build, the focused flow, and all 24 Playwright e2e tests pass. This test-only slice changes no product behavior, so no CHANGELOG or manual checklist is required.

- [x] Code-mode UX polish — disable Run while an entry/exit expression is invalid.
  - Entry and exit fields now share one synchronous validation result with the Run button, so either invalid expression blocks code-mode execution before the existing runtime fallback; dormant code expressions do not block params or blocks mode.
  - Added `aria-invalid` plus error-description links to both fields and a browser/mock regression covering invalid entry, mode switching, invalid exit, repairs, and a successful code-mode backtest.
  - `npm run typecheck`, 193 vitest, production build, and all 23 Playwright e2e tests pass. No Rust/SQLite paths changed, and no manual checklist is required because the browser flow owns the interaction.

- [x] REF-004 — refine `insert_strategy` UPSERT mutable-field semantics.
  - Same-hash re-saves now refresh `name`, `source`, and `updated_at`, while preserving the existing row id, definition-owned fields, and validation-owned `lifecycle` so a routine frontend save cannot demote a validated/rejected strategy to `candidate`.
  - Updated the existing no-duplicate test to cover rename/source refresh plus lifecycle preservation, and added a focused rename-persistence regression. No migration, hash, TypeScript, or UI changes.
  - `cargo check --locked`, 10 Rust tests, `npm run typecheck`, 193 vitest, production build, and all 22 Playwright e2e tests pass. No manual checklist is required because migrated in-memory SQLite tests own this repository behavior.

- [x] UI port — Slice 8b-2: real Tauri metrics OS window.
  - Extracted the shared full/Holdout metrics renderer so the existing inline and floating views and the new native child window use one formatter and column model.
  - Added a typed metrics ready/snapshot bridge, standalone child route, stable single-instance Rust window spec, and a separate least-privilege event capability scoped to `main` + `metrics-popout-window`.
  - `npm run typecheck`, 193 vitest, production build, `cargo check --locked`, 9 Rust tests, and all 22 Playwright e2e tests pass. Native window interaction remains a PR manual checklist because the Windows Computer Use native pipe was unavailable.

- [x] BUG-004 — backtest direction/input contract (Backtest Correctness Phase 3).
  - Restored legacy `both` reversal semantics for close and `nextOpen`: entry requests long, exit requests short, opposing positions close before opening the requested side, same-side signals hold, and entry wins a simultaneous entry/exit bar.
  - Core now rejects non-finite/out-of-range normalized sizing, fee, slippage, SL, and TP fractions instead of clamping them; UI/service percentage conversion and legacy fallbacks remain exclusively in `backtestRunner`.
  - Added 24 focused direction/validation tests and intentionally updated only the affected `both` golden trade count, last trade, and metrics. `npm run typecheck`, 190 vitest, production build, and all 21 Playwright e2e tests pass.

- [x] BUG-003 — backtest fill timing + risk exits (Backtest Correctness Phase 2).
  - `nextOpen` signals now create pending orders that execute on the following tested candle, so fills use the execution bar's timestamp/index and its open no longer leaks into the signal-bar equity point; final-bar signals do not fill beyond the tested range.
  - SL/TP exits now use gap-aware open/threshold prices plus the correct closing-side slippage for long and short positions, retain conservative SL-first handling without sub-bars, and apply normal exit slippage to EOD settlement.
  - Added 10 hand-calculated timing/risk/EOD tests and intentionally updated affected golden timestamps, risk-exit prices, and derived metrics. `npm run typecheck`, 166 vitest, production build, and all 21 Playwright e2e tests pass.

- [x] BUG-002 — backtest accounting + EOD settlement contract (Backtest Correctness Phase 1).
  - Adopted `docs/backtest-execution-contract.md`: normalized core units, fee-inclusive entry budget, long accounting, unleveraged 1× short collateral, settled metrics baseline/endpoint, and the approved BUG-003/004 follow-ups.
  - Corrected `ClosedTrade.pnl`/`pnlPct` to include both fees; 100% sizing now budgets entry fee without negative free cash; EOD replaces the final mark with settled equity; net return/CAGR/Sharpe/drawdown include configured starting equity.
  - Added six hand-calculated long/short/partial-size/multi-trade/EOD reconciliation tests and intentionally updated golden metrics. Trade count, fill timestamps, and fill prices remain unchanged in this phase.
  - `npm run typecheck`, 156 vitest, production build, and all 21 Playwright e2e tests pass.

- [x] TEST-002 — backtest engine golden tests + legacy parity report (audit backlog; no product-code edits).
  - Added four hard-coded golden configurations over `makeSampleCandles({ seed: 42, count: 300 })`, locking trade count, first/last trade time + price, net return, max drawdown, and Sharpe; added five boundary cases for same-bar signals, one candle, `from === to`, zero UI size, and negative UI costs.
  - Added `docs/engine-parity-report.md` with seven evidence-linked current/legacy comparisons and a follow-up BUG task template. It records recommendations only; maintainer decisions on engine semantics remain open.
  - `core/backtest/index.ts` is unchanged. `npm run typecheck` and all 150 vitest tests pass.

- [x] REF-003b — extract StrategySection; BacktestPanel becomes the orchestrator (PR #41; audit backlog, move-only).
  - Moved the strategy card (mode tabs, library picker, params/blocks/code editors, indicator/exec grids, Holdout toggle, Run) into `components/StrategySection.tsx`. **`BacktestPanel` 648 → 385 lines — the REF-003 `< 400` acceptance criterion is now met (finished per the REF-003 ultrareview).** This closes the audit refactor phase: the panel now only holds shared state + handlers and composes Chart / Dataset / Strategy / Results / Sweep sections.
  - Zero behaviour change: typecheck / 141 vitest / build / 21 e2e green; every strategy `data-testid` preserved.

- [x] REF-003 — extract Dataset/Results sections (PR #40; audit backlog, move-only).
  - `components/DatasetSection.tsx` (資料集 card) + `components/ResultsSection.tsx` (metrics table + export + save + metrics pop-out). `BacktestPanel` 811 → 648 lines. The `< 400` target was completed by the REF-003b follow-up (the strategy form + embedded library was the remaining large block).
  - Zero behaviour change: typecheck / 141 vitest / build / 21 e2e green.

- [x] REF-002 — extract ChartSection (PR #39; audit backlog, move-only).
  - Moved the chart concern (canvas + overlays + bar replay + hover 此根資訊 readout + quick param row + Slice 8a pop-out + Slice 8b native-window snapshot/cursor sync) into `components/ChartSection.tsx`; shared `components/PoppedOutNote.tsx` + `components/panelTypes.ts`. `BacktestPanel` 1047 → 811 lines. Rendered unconditionally so the always-on native-window "ready" listener registers exactly as the inline code did.
  - Zero behaviour change: typecheck / 141 vitest / build / 21 e2e green.

- [x] REF-001 — extract SweepSection from BacktestPanel (PR #37; audit backlog, move-only).
  - Moved the parameter-sweep block (state/handlers/`AxisEditor`/`SweepHeatmap`/JSX) into `components/SweepSection.tsx`; extracted shared `components/panelStyles.ts` (`S`), `components/NumberInput.tsx`, and `services/holdout.ts`. `BacktestPanel` 1382 → 1047 lines; every sweep `data-testid` preserved.
  - Zero behaviour change: typecheck / 141 vitest / build / 21 e2e green. (The rest of the decomposition — REF-002 ChartSection #39, REF-003 Dataset/Results #40, REF-003b StrategySection #41 — is now complete; see the entries above.)

- [x] BUG-001 — parameter sweep respects Holdout (PR #34; audit backlog `docs/improvement-backlog.md`).
  - When Holdout is on, the sweep now optimises on the in-sample segment only (shared `holdoutSplitIndex` with `run()`); Holdout-off keeps full-period behaviour unchanged.
  - Added a reactive in-sample scope note (`data-testid="sweep-scope"`) and a sweep e2e flow. typecheck / 141 vitest / build / 21 e2e green.
  - Follow-up (from ultrareview, low; PR #36): the sweep e2e now runs a Holdout-on sweep and asserts its winning combo differs from the full-period sweep, guarding the in-sample `from/to` wiring against silent removal.

- [x] DOC-001 — status single source of truth (PR #33; audit backlog).
  - Removed stale/contradictory status claims from README (中/EN/JP), AGENTS.md §0.1, and `alpha-factor-forge/TODO.md`; status now points here (Current Snapshot). Kept the "never `npm audit fix --force`" warning.

- [x] Project audit blueprint (PR #32).
  - Added `docs/project-audit-masterplan.md`, `docs/improvement-backlog.md`, `docs/creative-feature-roadmap.md`, `docs/agent-execution-protocol.md`. Analysis + agent-ready task specs; the backlog is a spec library, not a second task board (this file stays the single board).

- [x] Fix legacy saved-strategy loading compatibility.
  - Restored params rows saved before rule/code fields existed and blocks rows saved before code fields existed by filling only those historically absent, inactive-mode fields from current safe defaults.
  - Kept strict rejection for missing active-mode fields, partially missing field pairs, and malformed persisted values.
  - Added three regression cases; typecheck, 141 unit tests, build, 7 Rust tests, and 20 browser E2E tests pass on the post-Slice-8b-1 baseline.

- [x] UI port — Slice 8b-1 real Tauri chart OS window.
  - Added an async Rust single-instance/focus command for a resizable native chart window, avoiding the documented synchronous-command WebView2 deadlock on Windows.
  - Added a chart-only child mount and typed ready/snapshot/cursor event bridge; replay ticks send cursor-only updates instead of the full candle dataset.
  - Preserved child-local chart zoom/pan by retaining candle identity for same-dataset snapshot updates.
  - Added a least-privilege Tauri capability for the main/chart event handshake and a regression test covering both window labels and all required event permissions.
  - Added frontend/Rust tests and child-route E2E; real second-window click/open remains a manual Tauri checklist because Windows UI automation was unavailable.

- [x] UI port — Slice 10-2 chart drag-pan.
  - Added pointer-captured horizontal panning for zoomed charts with whole-bar clamping at dataset and replay boundaries.
  - Preserved Slice 9 hover/crosshair behavior for short clicks by requiring a 4px drag threshold; grab/grabbing cursors communicate the interaction.
  - Corrected replay playhead rendering for historical panned windows and resume-follow behavior at the cursor boundary.
  - Added pure pan-window tests and real pointer-drag Playwright coverage without canvas pixel assertions.

- [x] UI port — Slice 10-1 chart wheel zoom.
  - Added a cursor-anchored visible-window zoom with explicit visible-bar count and reset-to-fit control.
  - Kept bar replay bounded at its cursor while preserving the selected zoom level; dataset/replay mode changes return to a predictable fit window.
  - Fixed wheel event handling with a non-passive native listener so zooming does not move the surrounding page.
  - Added pure window-math unit coverage and real wheel-input Playwright coverage without relying on canvas pixel assertions.

- [x] UI port — Slice 7-3 strategy library.
  - Added an SQLite-backed saved-strategy picker with refresh and load actions; saving a backtest refreshes and selects the saved row.
  - Added strict persisted-definition validation before restoring params, blocks, or manual code strategies into the editor.
  - Added unit and browser E2E coverage; Playwright accepts an `E2E_PORT` override with strict port binding so unrelated local dev servers cannot be reused accidentally.

- [x] Improve button press feedback and export download status.
  - Added global button hover/active/focus/disabled feedback for the React app.
  - Added explicit JSON/CSV export status messaging (`正在準備...` / `下載完成...`) beside the export buttons.
  - Updated `e2e/export.spec.ts`; `npm run typecheck`, `npm test` 125, `npm run build`, and `npm run e2e` 14 passed.
- [x] Browser E2E harness foundation + first regression (reduces manual UI testing).
  - `dataClient` seam: production/Tauri uses the real `tauri-client`; in Vite DEV only, `?mock=1` swaps in an in-memory mock (`mockClient`, seeded sample candles — no localStorage, no real DB; dead-code-eliminated from prod).
  - Playwright (chromium) running against `npm run dev`; CI `e2e` job; `npm run e2e` locally. Vitest scoped to `src` so it ignores `e2e/`.
  - First test `e2e/holdout.spec.ts`: Slice 5a Holdout stale-UI flow (load sample -> enable -> run -> 3 columns -> disable -> single column) + `data-testid` hooks.
  - Second test `e2e/sweep.spec.ts` (Slice 5b-2): load sample -> expand 參數掃描 -> combo count -> run -> heatmap best cell -> apply best.
  - Explicitly does NOT replace real Tauri/Rust/SQLite verification (Rust integration tests + `cargo tauri dev` smoke still own that).
- [x] Automate the blocks-save verification (Slice 4a follow-up; replaces manual SQLite checks).
  - TS: strengthened `buildStrategyDef` tests — a blocks rules strategy persists `type='blocks'`, `JSON.parse(original_definition_json).mode === 'blocks'` with the rules intact, and params/blocks `strategy_hash` differ.
  - Rust: `repositories::tests` integration tests on an in-memory migrated DB — `insert_strategy` round-trips `type='blocks'`, and a same-hash re-save does not duplicate (documents the current UPSERT-only-`updated_at` behavior).
  - CI: added `cargo test --locked` to the `cargo-check` job so the Rust test runs on every PR. No schema change; no code mode. Manual SQLite Viewer checks are no longer the acceptance gate.
- [x] Prepare the local Tauri verification environment.
  - Installed Rust/Rustup/Cargo 1.96, MSVC C++ build tools (VS Build Tools 2022), and Tauri CLI v2.
  - Generated multi-size icons (`icon.png`/`.ico`/`.icns` + platform sets) via `tauri icon`.
  - `cd alpha-factor-forge/src-tauri && cargo check` passes; PR CI `cargo-check` job also green.
- [x] Launch the Tauri Phase A bridge locally with `cargo tauri dev`.
  - Native window opens; title bar shows the app + icon.
  - Status reads `database already initialized at startup`; SQLite created in OS app-data; bridge lists datasets (0 initially, expected).
  - Verified end-to-end via the bridge-shell self-test: save->read round-trip + upsert returned PASS.
  - Note: use `npm run tauri -- dev` if `cargo-tauri` is not installed as a cargo subcommand (`cargo install tauri-cli` enables `cargo tauri dev`).
- [x] Complete Phase A backtest result persistence (core).
  - Added `repositories::insert_backtest_summary` (upsert on strategy+dataset+segment) and `list_backtest_summaries`.
  - Wired `save_backtest_result` (now takes a typed `BacktestSummary`, not a raw JSON string) and `get_backtest_results`.
  - Added the `BacktestSummary` interface to `tauri-client/commands.ts`; `npm test` + `npm run typecheck` green.
  - Still needs local `cargo check` (no Rust toolchain in the authoring env). `trades`-table detail deferred to the UI port.
- [x] Add Tauri app icon (`src-tauri/icons/icon.png` + `app-icon-source.png`); `tauri.conf.json` references it.
- [x] Review the project archive and capture initial project context in `AGENTS.md`.
- [x] Copy `HISTORY.md` and `CONVERSATION_HISTORY.md` into the workspace.
- [x] Establish the canonical local source tree from `區塊鏈交易策略PWA.zip`.
  - Preserved the archived `alpha-factor-forge/` scaffold structure intact.
  - Preserved `AlphaFactorForge.dc.html`, `STRATEGY_DISCOVERY.md`, `STRATEGY_GUIDE.md`, screenshots, uploads, and prototype notes as reference material.
  - No same-path file conflicts were found during extraction, so no existing files were overwritten.
  - Root `README.md` and `.gitignore` were integrated as workspace-level files while keeping `alpha-factor-forge/README.md` and `alpha-factor-forge/.gitignore`.
- [x] Run the TypeScript baseline verification for the scaffold.
  - Installed npm dependencies in `alpha-factor-forge/`.
  - `npm test` passed: 3 test files, 25 tests.
  - `npm run typecheck` passed after narrow TypeScript strict-mode fixes.
  - `npm run build` passed and produced `alpha-factor-forge/dist/`.
  - Started local inspection servers: scaffold on `http://127.0.0.1:5173/`, legacy prototype on `http://127.0.0.1:5174/AlphaFactorForge.dc.html`.
- [x] Merge the legacy `task.md` feature map into this active `tasks.md` board.

## Legacy PWA Feature Map

This section preserves the useful planning content from the former `task.md`. It describes what the prototype already demonstrated, not what has been ported to Tauri.

### Market Data

- Historical candles and live prices via Binance / OKX / Coinbase fallback.
- Multiple intervals: 1m / 3m / 5m / 15m / 1h / 4h / 1d.
- Manual data source selection and visible active source.
- Deep historical paging: 500 / 2000 / 5000 / max available.
- Dataset export/import as JSON/CSV for reproducible frozen backtests.

### Strategy Definition

- Params mode.
- Rule-block mode.
- Manual JavaScript expression code mode.
- Built-in example strategies for learning/testing.
- Indicator operands include MA, EMA, RSI, MACD, Bollinger, ATR, stochastic, volume MA, and price fields.

### Execution Model

- Fees/commission.
- Slippage.
- Position sizing.
- Fill assumption: current close vs next open.
- Direction: long, short, both/reversal.
- Stop-loss / take-profit.
- Bar Magnifier for intrabar SL/TP ordering.
- No-future-function discipline by using closed-bar data.

### Performance And Analysis

- Net return, buy-and-hold comparison, win rate, trade count, max drawdown, profit factor, average trade, ending equity.
- Sharpe, Sortino, Calmar.
- Average win/loss, win/loss ratio, expectancy, max win/loss streaks, largest win/loss, average holding bars, time in market.
- Equity curve, buy-and-hold overlay, underwater drawdown.
- Round-trip trade table with MAE/MFE.
- Focus Data mode that collapses the chart and enlarges statistics.

### Robustness And Workflow

- Real date-range backtesting.
- Holdout / sample-out comparison.
- Parameter sweep heatmap.
- Bar Replay.
- Strategy library in localStorage.
- Backtest report JSON export and trade CSV export.

### Remaining Legacy/PWA Options

- Walk-forward analysis.
- Multi-asset portfolio backtesting.
- Alerts/webhooks.
- Service Worker if the PWA line is kept alive.
